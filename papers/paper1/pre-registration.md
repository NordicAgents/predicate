# Pre-registration — Conflict-Complete Retrieval under Bounded Context (mechanism-v0)

**Version:** v1.0
**Date:** 2026-07-12
**Status:** FROZEN. This document must be AMENDED (dated sections appended under §10), never edited or rewritten. Any change to hypotheses, metrics, fixtures, exclusions, or analysis code after this date requires an amendment entry; silent edits invalidate the pre-registration.
**Registered against:** git tag `evidence-snapshot-2026-07-12` (commit `3b63c1e`), branch `paper1-evidence-sprint`.
**Governing plan:** `papers/paper1/top-venue-publication-plan.md` §7–§9 and Phase 0 (§11), which mandates: "Pre-register hypotheses, primary metrics, exclusions, and the analysis script before headline runs."

---

## 1. Frozen primary question

Aligned with plan §1:

> **What information must an agent-memory retrieval policy index to be conflict-complete under a bounded context budget — and at what context cost does each policy return the source-grounded conflict witness?**

Narrowed operational form for this registration (mechanism-v0 scope):

> For each retrieval policy π and context budget B, on what fraction of gold conflict instances does π's returned context contain the complete gold witness (both source records / all gold witness triples), and what does that containment cost in triples, bytes, and milliseconds — compared against exact symbolic baselines that index the key relation directly?

Explicit **non-claims** at this stage (per plan §3 and Gate A):

- No claim that the reasoner is more *accurate* than exact key-join / SPARQL baselines on mechanism-v0 (H4/H5 below predict the opposite of any such claim).
- No claim of external validity: all current fixtures are synthetic mechanism demonstrations (`mechanism-v0`); realism claims are deferred to the Phase 2 benchmark.
- No claim about LLM reasoning ability; LLM arms measure the *interface* (what a reader can see given a policy's context), not model intelligence.
- No RAG-competition framing.

## 2. Measurement contract this registration binds to

The instance-level contract being built this sprint (headline runs may not start before it is frozen; see §9):

- **Fixtures (frozen at tag `evidence-snapshot-2026-07-12`):**
  - `packages/predicate-eval/fixtures/conflict-d20/` — same-subject conflicts (d=0.20).
  - `packages/predicate-eval/fixtures/conflict-xr-small/` — cross-record conflicts, co-reference only via shared email key literal (60 persons).
  - `packages/predicate-eval/fixtures/conflict-xr-scale/` — same mechanism, 300 persons.
  - All are **synthetic mechanism-v0**. The generator deliberately places the twin record outside the IRI-only BFS ball; this is a designed property, disclosed, and is why H1 is a mechanism check, not a discovery (§2.1).
- **Instance records:** `packages/predicate-eval/fixtures/<domain>/instances.json`, one record per instance with `kind ∈ {conflict, benign-coreference, benign-shared-value, benign-duplicate, benign-multivalued}`, the gold witness triple set, and the entity (key-equivalence class) it belongs to. Positives = `conflict`; all `benign-*` kinds are hard negatives.
- **System outputs:** one `PredictionRow` per (system, instance): predicted conflict yes/no, returned/decisive values, returned context (triples and bytes), `costMs`, and raw output for LLM arms.
- **Systems registered for the comparison runs:**
  1. `exact-key-join` (hash join on the declared key + distinct-value check)
  2. `sparql-groupby` (GROUP BY key HAVING COUNT(DISTINCT value) > 1)
  3. `reasoner-r14r23r22` (hasKey→sameAs chain, value propagation, conflict flag)
  4. `retrieval:iri-bfs@k` — IRI-only BFS ball, witness-containment semantics
  5. `retrieval:literal-aware@k` — BFS that also traverses shared literals
  6. `retrieval:key-aware@k` — traversal only across declared-key literals
  7. Mem0 ingest arms — **exploratory to date** (§8); any confirmatory rerun requires an amendment
  8. Pinned-LLM flat arms (`flat-all`, `flat-retrieved`) — infrastructure ready, **no API keys yet**; these run under this registration only if fixtures/metrics are unchanged when keys arrive, otherwise via amendment.
- **Witness-containment semantics for retrieval arms:** a retrieval policy is scored on whether its returned context *contains all gold witness triples* for the instance — i.e., whether a sound detector reading that context could establish the conflict. Retrieval arms are never penalized for lacking a detector; symbolic and LLM arms are scored on the emitted conflict decision.

### 2.1 Prior-knowledge disclosure (what is already known that bears on the hypotheses)

Honesty requirement: Pilot E's deterministic k-sweep (EXPERIMENT-LOG.md, 2026-07-11) already established pair-level reach for IRI-BFS: twin reach 0.000 at k≤3, 1.000 at k=4, on conflict-xr fixtures. **H1 is therefore a registered mechanism check under the new instance-level witness-containment metric, not a novel prediction.** H2, H3, and H4 concern systems that exist but whose comparison runs have **not** landed as of this date (no results files exist for exact-key-join, sparql-groupby, key-aware, or literal-aware arms). H5's equality prediction is registered before any reasoner-vs-exact-baseline paired run exists.

## 3. Named hypotheses (directional, registered before the comparison runs land)

- **H1 (mechanism).** `retrieval:iri-bfs@k` for k ≤ 2 achieves a witness-complete rate of **exactly 0** on the cross-record fixtures (`conflict-xr-small`, `conflict-xr-scale`): no conflict instance's full gold witness set is contained in any k≤2 IRI-only ball seeded at either record. (Status: mechanism check — see §2.1.)
- **H2 (sufficiency of key indexing).** `retrieval:key-aware@1` and `retrieval:literal-aware@1` both achieve witness-complete rate **1.0** on the same cross-record fixtures: one hop across the shared key literal reaches the twin record, so the full witness is contained at k=1.
- **H3 (cost separation, direction only).** `literal-aware@k` pays a **context-size premium** over `key-aware@k` (more triples and more bytes per instance at equal k), and this premium **grows monotonically with the number of non-key shared literals** in the store (literal-aware traverses every shared literal; key-aware traverses only declared keys). Direction only is registered; no effect size is predicted. On benign-shared-value instances the premium is predicted to be largest.
- **H4 (the null result that forces the generality argument).** `exact-key-join` achieves conflict **precision = recall = 1.0** on all three current fixtures at **< 10 ms median per-instance `costMs`**. Consequence, registered in advance: on mechanism-v0, *no accuracy-based superiority claim for the reasoner (or any other system) over a trivial exact join is admissible*. If H4 holds, the paper's argument must rest on generality, guarantees, or cost on richer data (plan §11 Gate A), not on mechanism-v0 accuracy.
- **H5 (reasoner parity, edge deferred).** `reasoner-r14r23r22` **matches** exact-baseline accuracy on all current fixtures (predicted paired per-instance F1 difference vs `exact-key-join` = 0). The reasoner's claimed edge — rule-chain generality beyond single joins, τ/σ (time/scope) conditioning, provenance gating — is **not testable on mechanism-v0**, and no such claim will be made from these runs; it is deferred in full to the realistic benchmark (plan Phase 2).

Falsification is symmetric and binding: if H2 fails (key-aware misses witnesses) the sufficiency story is wrong as stated; if H4 fails in the accuracy direction (exact join < 1.0) the fixtures or the join are broken and an amendment plus root-cause entry is required before any further runs; if H5 fails in either direction it is reported as-is.

## 4. Primary vs secondary metrics

**Primary (exactly three; all instance-level):**

1. **Conflict F1** per system per fixture: precision/recall/F1 over the binary per-instance conflict decision, positives = `conflict` instances, negatives = all `benign-*` instances. (Precision and recall are reported; F1 is the single headline number.)
2. **Witness recall**: fraction of gold witness triples contained in the system's returned context / cited evidence, averaged over conflict instances; and the derived **witness-complete rate** (fraction of conflict instances with witness recall = 1.0), which is the quantity named in H1/H2.
3. **B-bounded completeness curve**: for each retrieval policy, witness-complete rate as a function of context budget B, computed on a fixed grid of B in **triples** {5, 10, 25, 50, 100, 250, 500, 1000, whole-store} and mirrored in **bytes**; a policy's point at B uses only instances where its returned context ≤ B. The curve is the primary cost-completeness object; single-k points (H1/H2) are cross-sections of it.

**Secondary (reported, never headlined, no hypothesis may be promoted to them post hoc):**

- Both-value recall (both conflicting values present in returned context/answer).
- Context cost: triples and bytes per instance (absolute, and premium ratios for H3).
- `costMs` per instance (p50/p95 per system).
- Parse/format compliance rate for LLM arms (separate metric per plan §9; see §6).
- Benign-kind breakdown (false-positive rate per `benign-*` kind).
- Aggregate 8-question scores from the legacy harness (kept only for continuity with pilots; never a headline).

## 5. Experimental units and clustering

- **Unit of analysis: the instance.** Never the seed, never the run, never the question-set aggregate. Random seeds of the same generator are regression checks, not replications (plan §9), and contribute no degrees of freedom.
- **Fixtures are strata, not independent domains.** All three fixtures come from one synthetic generator family (mechanism-v0); results are reported per fixture but pooled claims across fixtures are labelled single-domain. True domain-level clustering begins only when Phase 2 realistic domains exist.
- **Paired comparisons only.** Every system runs on the identical instance set; system contrasts (e.g., H5's reasoner vs exact-key-join) use paired per-instance differences — McNemar's test for binary decisions, paired bootstrap for F1/witness-recall differences.
- **Cluster bootstrap by entity.** Uncertainty intervals resample **entities** (key-equivalence classes / persons), not instances, since multiple instances can share an entity and are not independent. 10,000 resamples, seed fixed at 20260712, percentile intervals. Degenerate all-identical outcomes are reported as "identical outcome on N instances across M entities", never as [x, x] confidence intervals (Pilot E lesson).
- Effect sizes with intervals are reported for every contrast; significance alone is never reported.

## 6. Exclusion rules

1. **Parse failures are counted, never dropped.** For LLM arms, an unparseable output is recorded as `parse_failure` in the PredictionRow. Primary metrics are computed under the **strict** rule registered here: a parse failure counts as *no conflict detected* (a miss on positives, a correct-rejection contribution on negatives is NOT granted — the instance is scored as incorrect for the arm). Compliance rate is reported alongside as its own secondary metric. No "parseable-only" variant may be headlined; if computed, it is labelled diagnostic.
2. **No instance-level exclusions of any kind post hoc.** Every instance in `instances.json` at the frozen tag is scored for every system. Systems that cannot run a fixture produce an explicit `not-run` row with reason, reported in the results table.
3. **No post-hoc fixture edits.** mechanism-v0 is frozen at tag `evidence-snapshot-2026-07-12`. If a fixture or oracle bug is discovered after any comparison run, the fix requires a dated amendment (§10), a new tag, and a complete rerun of **all** systems; both pre-fix and post-fix results are retained and reported.
4. **No silent reruns.** Every run of the analysis pipeline appends to results; superseded runs are moved to `packages/predicate-eval/results/archive-*/` (existing convention), never deleted.
5. Infrastructure failures (OOM, disk-guard aborts) are logged with cause and the cell is marked `incomplete`, never backfilled with a partial score.

## 7. Analysis plan (scripts and exact file paths)

The scoring code must be frozen (committed, hash recorded via amendment §10) **before** the first headline comparison run. Planned contract, binding once frozen:

| Artifact | Path | Role |
|---|---|---|
| Instance fixtures | `packages/predicate-eval/fixtures/{conflict-d20,conflict-xr-small,conflict-xr-scale}/instances.json` | Gold instances, kinds, witness triples, entity ids |
| System runners | `packages/predicate-eval/src/contract/run-systems.ts` (invoked via `npm run contract-run` in `packages/predicate-eval/`) | Emits one PredictionRow JSONL per (system, fixture) |
| Prediction rows | `packages/predicate-eval/results/contract/<fixture>.<system>.predictions.jsonl` | Raw per-instance outputs incl. context, costMs, raw LLM text |
| Scoring script | `packages/predicate-eval/src/contract/score.ts` (invoked via `npm run contract-score`) | Computes §4 primary + secondary metrics from prediction rows + instances.json; no other code path may produce headline numbers |
| Scored tables | `packages/predicate-eval/results/contract/scores.<fixture>.json` and `packages/predicate-eval/results/contract/scoreboard.md` | Per-system metric tables, B-curves, paired contrasts with cluster-bootstrap intervals |
| Statistical contrasts | computed inside `score.ts` (McNemar, paired/cluster bootstrap, seed 20260712) | H1/H2/H4/H5 verdicts printed explicitly as PASS/FAIL against §3 |

`score.ts` responsibilities, registered now: (a) conflict P/R/F1 per §4.1; (b) witness recall and witness-complete rate per §4.2; (c) B-bounded curves on the §4.3 grid; (d) H1–H5 verdicts; (e) secondary metrics; (f) strict parse-failure handling per §6.1. Existing scripts (`src/conflict/xr-stats.ts`, `src/flat*.ts`, `baselines/*.sh`) remain pilot infrastructure and may feed PredictionRows but may not compute headline metrics.

## 8. What is exploratory (hypothesis-generating, already run, NOT pre-registerable)

Everything in `papers/paper1/EXPERIMENT-LOG.md` to date is exploratory and may only be described as such in the paper:

- **Pilot A** (org three-way; the org-q02 information-asymmetry artifact).
- **Pilot B** (CONFLICT-BENCH v1 density sweep; the same-subject perfect tie).
- **Pilot C** (conflict-xr-small three-arm; flat-retrieved 0.58).
- **Pilot D / D2** (Mem0 classic ingest on d20 and xr-small; 0/8 preserved, 0/12 linked) and the Mem0 2.x additive arm (1/8) — all Mem0 runs to date, including `baselines/results/mem0-ops.*.jsonl`.
- **Pilot E** (5-seed replication, k-sweep, xr-scale; degenerate-interval lesson).
- **Pilot F** (p1000/p2000 flat-all cost sweep; pattern-shortcut observation).
- **Pilot G** (model-tier matrix; fair-parser/ctx fixes).
- All in-session frontier-subagent results (not pinned, not independently reproducible — plan §3).

These motivated the hypotheses in §3; none of them may be presented as confirmatory tests of those hypotheses. Confirmatory status attaches only to runs executed **after** this registration is committed and the §7 scoring script is frozen.

## 9. Stopping and deviation rules

1. **Start gate.** Headline comparison runs may begin only when all of: (a) `instances.json` exists for all three fixtures at the frozen tag (or an amended tag), (b) `src/contract/score.ts` is committed and its hash recorded in an amendment below, (c) this document is committed. Runs started before the gate are exploratory by definition.
2. **Amendment-only changes.** Any change to fixtures, instance definitions, metrics, exclusion rules, hypothesis wording, or scoring code after the start gate requires a dated amendment section under §10 stating what changed, why, what had already been observed at the time, and which results are invalidated/rerun. The original text above is never edited (typo fixes included — note them in an amendment).
3. **Fixture-bug rule.** Discovery of a generator or oracle bug stops all runs; §6.3 applies.
4. **No optional stopping on LLM cells.** The number of repeats for stochastic LLM arms is fixed before the first repeat of that cell runs and recorded by amendment; cells are never extended because the current estimate looks unfavorable.
5. **Pinned-LLM arms.** When API keys arrive, model IDs, prompts, sampling parameters, and repeat counts are registered by amendment *before* the first pinned run; in-session subagent results remain exploratory forever.
6. **Gate A consequence, pre-committed.** If H4 holds (expected), the mechanism-v0 accuracy story is closed as a null result and the project proceeds per plan §11 Gate A (generality/guarantee/cost or pivot to the benchmark/audit paper). No amendment may reframe mechanism-v0 accuracy as a headline win.

## 10. Amendments

*(Append dated entries below. Never edit sections 1–9.)*

### Amendment A1 — 2026-07-12 (same-day: implemented paths, schema freeze, timing disclosure)

**What changed.** The §7 analysis plan named planned paths (`src/contract/run-systems.ts`, `src/contract/score.ts`, `results/contract/*.predictions.jsonl`). The sprint implemented the same contract under different names, now frozen:

- Instance manifests: `packages/predicate-eval/fixtures/<domain>/instances.json`, built by `src/instances/build-manifest-cli.ts` (manifest logic `src/instances/manifest.ts`, sha256 `fa2d95a84eb1…`). All three fixtures carry manifests; recorded explicitly: `conflict-xr-scale` is a full stratum with its own `instances.json`, not a cost-only stratum.
- Scoring script: `src/instances/score-cli.ts` (sha256 `0967623a0112…`; includes a loud warning when any system has instances with no prediction row, so manifest-id drift can never again be silently scored as un-flagged).
- Schema note: `InstanceRecord` carries no explicit `entity` field; the §5 cluster-bootstrap-by-entity procedure derives the entity as the shared `key` literal (v2 fixtures) or the single `subjects[0]` (v1 fixtures).
- Systems and prediction outputs: `src/exact/run-exact-cli.ts` → `results/exact/<system>.<domain>.jsonl`; `src/rigs/retrieval-policies-cli.ts` → `results/retrieval/retrieval.<domain>.jsonl`; `src/instances/reasoner-arm-cli.ts` → `results/instances/reasoner-r14r23r22.<domain>.jsonl`; scores → `results/instances/summary.<domain>.json`.
- PredictionRow field schema frozen as implemented: `{instanceId, domain, system, flagged, values[], witness[] ("s|p|o" ids), costMs, extra{}}`; InstanceRecord as in `src/exact/contract.ts`.
- One-command rebuild: `scripts/build-evidence.sh` (stages: fixtures-drift-check, deterministic, summary → `papers/paper1/evidence/summary-<sha>.json`).

**Timing disclosure (required by §9.1–9.2).** This registration was drafted by an agent running **in parallel with** the implementation agents on 2026-07-12; the first full comparison runs executed the same day, before this document was committed. By §9.1 those runs are therefore **exploratory**. Because every registered system is deterministic (hash join, SPARQL GROUP BY, witness-containment, fixpoint), the post-commit rerun of `scripts/build-evidence.sh` constitutes the confirmatory record; its decision values must (and do) reproduce exactly, with only timing fields varying. H1, H2, H4, H5 outcomes observed today: all in the registered direction. H3 was **not testable**: `cb2:email` is the only literal in the current aboxes, so literal-aware and key-aware balls are provably identical on mechanism-v0 (verified by per-seed set equality); testing H3 requires a fixture with non-key shared literals (registered here as a required Phase-1 fixture addition, subject to the §6.3 no-silent-rewrite rule — it must be a NEW fixture, not an edit of mechanism-v0).

**Known deviation — reconciled before landing.** During the sprint the exact arm derived per-subject `benign-shared-value` probes (17 rows on conflict-xr-small, 65 on conflict-xr-scale) vs the canonical paired-instance manifests (16 / 64), so the scorer reported `missing=1` for exact systems on xr domains, counted as un-flagged benign (TN; conflict P/R/F1 unaffected). The exact arm was reconciled to the canonical paired-instance ids in the landing commit; `missing=0` for every system on every domain in the committed artifacts.

### Amendment A2 — 2026-07-13 (Phase-1 registration: chain-depth, τ/σ, and H3-capable fixtures; extended exact baseline; hypotheses H6–H8)

**Context.** Gate A was decided 2026-07-13: Option 1, proceed reframed (method paper on conflict-complete retrieval under bounded context), per `gate-a-analysis.md` §6, under the binding conditions of its §5(a)–(c). This amendment registers the Phase-1 fixture family, the extended exact baseline, and the new hypotheses **before** any of the fixtures, systems, or runs exist. mechanism-v0 remains frozen and untouched (§6.3): everything below is NEW fixtures and NEW system code.

**Timing disclosure (required by §9.1–9.2).** At the time of drafting, no `generate-v3` code, no v3 fixture data, and no extended-baseline code exists in the repository; the designs are informed by the (closed) mechanism-v0 results and by `formal-conflict-completeness.md`. Predictions marked *(mechanism check)* below are guaranteed by fixture construction — registered for honesty, not as discoveries — exactly as H1 was in §2.1. All other predictions are forward-looking.

**A2.1 New fixture family (`phase1-v3`), generator `src/conflict/generate-v3.ts`.**
Deterministic (mulberry32, fixed seed recorded in the generator source), namespace `cb3: = http://ex/cb3#`, same file set as v2 (`world.ttl`, `episodes/*.jsonl`, `oracle.json`, `questions.json`) plus the derived `instances.json`. Registered fixtures:

1. **`conflict-chain-m2`** (40 person-chains) and **`conflict-chain-m3`** (24 person-chains): each instance is a ∼K-chain of m+1 records across sessions — two *endpoint* records carrying the constrained value (`cb3:office`) and m−1 *sparse intermediate* records carrying **two** key values (`cb3:email`) each and **no** constrained value. Conflict instances have differing endpoint values; benign-coreference chains agree. By construction every conflict witness has chain length m (2 or 3) and cardinality **|W| = 3m+3** (see A2.5). Sparse intermediates carry no IRI-object triples, so they are isolated vertices of the IRI graph G(S) — d(endpoint, intermediate) = ∞ through IRIs. `benign-shared-value` probe instances are included as in v2. Conflicted fraction: 10/40 (m2), 6/24 (m3).
2. **`conflict-h3-nk1`** and **`conflict-h3-nk3`** (60 persons each, 12 conflicted, mirroring conflict-xr-small): the v2 cross-record twin mechanism unchanged, PLUS **nk ∈ {1, 3}** non-key shared-literal predicates (`cb3:city`; + `cb3:title`, `cb3:building` at nk=3) whose string values are shared across groups of ~10 person-records. These make the literal-aware and key-aware balls provably different, making H3 testable (registered as required by Amendment A1).
3. **`conflict-tausig`** (60 persons; 12 conflict, 12 benign-temporal, 12 benign-scoped, 24 benign-coreference): τ/σ carried as **record-level annotation triples** — `cb3:validFrom`/`cb3:validTo` (ISO date literals; interval [from, to), absent = ⊥) and `cb3:sourceScope` (string literal; absent = ⊥) on the record subject; every assertion of a record inherits its record's τ/σ (the record-as-snapshot encoding used by realistic registries, cf. `domain-selection.md` C2/C4). Kinds: `conflict` = co-keyed records, overlapping-or-⊥ τ AND equal-or-⊥ σ, differing constrained value; `benign-temporal` = disjoint τ intervals (temporal supersession), differing value; `benign-scoped` = differing σ, differing value; `benign-coreference` = agreeing values.

**A2.2 Schema extension.** `InstanceKind` gains `benign-temporal` and `benign-scoped` (hard negatives, exactly like the existing `benign-*` kinds). `InstanceRecord` and `PredictionRow` are otherwise unchanged. The scorer (`src/instances/score-cli.ts`) is reused; its per-kind breakdown covers the new kinds with no metric changes. Verdicts for H3/H6/H7/H8 are computed by a new script `src/instances/phase1-verdicts-cli.ts` from prediction rows only; its hash is recorded at the landing commit (as A1 did for the scorer).

**A2.3 New system: `exact-key-join-x`** (the Prop. 4 *full-fragment* exact baseline). Per-(keyed-class, key-value) bucketing + a **union–find** closure over co-key pairs (O(n·α(n))), then per-equivalence-class, per-constrained-predicate distinct-value check **partitioned by τ/σ overlap**: two values conflict only if their records' τ intervals overlap (⊥ overlaps everything) and their σ are equal-or-⊥ — i.e., F3's side conditions implemented exactly. It runs on ALL domains (mechanism-v0 + phase1-v3) so cost and accuracy are paired against `exact-key-join`, `sparql-groupby`, and `reasoner-r14r23r22`. The existing three detection systems run UNCHANGED on the new fixtures; their predicted failures below are registered predictions, not bugs, and must be reported as measured.

**A2.4 New hypotheses (directional, registered before any v3 fixture or run exists).**

- **H6 (chain depth).** On `conflict-chain-m{2,3}`: (i) *(mechanism check)* the single-join detectors `exact-key-join` and `sparql-groupby` detect **0** of the chain conflicts (recall 0, no false positives) — structural, since no single key-value bucket contains two distinct constrained values; (ii) `exact-key-join-x` and `reasoner-r14r23r22` both achieve P = R = 1.0 (Prop. 2 / Prop. 4 full fragment); (iii) `retrieval:key-aware@k` has witness-complete rate **0 for k < m and 1.0 for k ≥ m** (completeness budget grows with chain length); (iv) *(mechanism check)* `retrieval:iri-bfs@k` has witness-complete rate **0 at every k ∈ {1..4}** — stronger than on mechanism-v0, because sparse intermediates are IRI-isolated, so no hop budget recovers them.
- **H7 (τ/σ conditioning).** On `conflict-tausig`: the τ/σ-blind detectors (`exact-key-join`, `sparql-groupby`, `reasoner-r14r23r22`) flag **all** `benign-temporal` and `benign-scoped` instances (false-positive rate 1.0 on those kinds; conflict precision ≈ 12/36 = 0.333 if recall holds at 1.0), while `exact-key-join-x` achieves P = R = 1.0 with zero benign false positives. Scope: H7 binds the detection systems only; retrieval arms are witness-containment-scored and carry no τ/σ prediction here.
- **H8 (extended-baseline cost, direction only).** `exact-key-join-x` keeps an end-to-end cost advantage of at least one order of magnitude over `reasoner-r14r23r22` materialization on every fixture where both are accurate, and stays within one order of magnitude of plain `exact-key-join` on mechanism-v0. Registered per Gate A condition 5(a)'s honesty clause: if the extended baseline is *also* complete and cheapest on the deeper fragment, that result stands and bears on Gate B falsification clause 3 (`gate-a-analysis.md` §5).
- **H3 (unchanged wording, §3), now operationalized.** Target fixtures `conflict-h3-nk1`/`conflict-h3-nk3`: mean returned-context triples and bytes of `literal-aware@1` strictly exceed `key-aware@1` on conflict instances (premium > 1×), the premium at nk=3 strictly exceeds the premium at nk=1 (monotonicity in non-key shared literals), and the premium is largest on `benign-shared-value` instances. Witness-complete rates of both policies remain 1.0 at k=1 (H2 carries over).

**A2.5 Correction of record (witness cardinality formula).** `formal-conflict-completeness.md` §2 and `gate-a-analysis.md` §5(a) state |W| = 4m + 2 for chain-length-m witnesses. As a **set** cardinality this double-counts the type assertions of intermediate records shared between adjacent F1 applications; the correct count is **|W| = 3m + 3** (m+1 type assertions + 2m key assertions + 2 value assertions; both formulas agree at m = 1, where |W| = 6). The formal doc and gate doc are corrected in the same commit as this amendment; no frozen text in §1–§9 of this registration stated the formula, so no §1–§9 result is affected.

**A2.6 Protocol carry-over and exclusions.** Primary metrics, B-grid, paired-comparison and cluster-bootstrap rules (§4–§5), and exclusion rules (§6) apply unchanged to the new fixtures; the entity for cluster bootstrap on v3 fixtures is the person id (one chain/twin group per person). The legacy tier-1 8-question `eval` stage is NOT run on phase1-v3 fixtures (it is a mechanism-v0 continuity metric only). `scripts/build-evidence.sh` gains the five new domains, the v3 generator in its drift check, the `exact-key-join-x` arm on all domains, and the verdicts script; mechanism-v0 stages are byte-unchanged.

**A2.7 Exploratory registration: Gate C preview probe (condition 5(c)).** A prevalence probe on domain C1 (vulnerability records, `domain-selection.md` §6): group records in a dated OSV bulk export slice (PyPI ecosystem) by shared CVE alias — records co-referent only through the CVE key — and report (a) the fraction of CVE-keyed groups with ≥ 2 independently authored records, and (b) the disagreement rate on severity and affected-range fields within co-keyed groups. This is **exploratory / hypothesis-generating** (it estimates prevalence before Phase-2 density decisions, per plan §11); no confirmatory status is claimed, and no benchmark instance is built from it under this registration.

### Amendment A3 — 2026-07-13 (Phase-1 method arm: Conflict Witness Index as a retrieval index; hypotheses H9–H10; clause-3 decision rule)

**Context.** The A2 runs landed with H3/H6/H7/H8 all in registered direction, and with the honest consequence recorded in `EXPERIMENT-LOG.md`: the extended exact baseline (`exact-key-join-x`) is complete AND cheapest as a *store-side detector* on every fixture, so the first conjunct of Gate-B falsification clause 3 (`gate-a-analysis.md` §5.3) is TRUE. The Phase-1 method therefore competes **only as a retrieval index** — on witness-completeness per context budget and on the maintenance ledger (formal doc Def. 5.1/5.3) — never on detection accuracy or store-side detection cost. This amendment registers that arm BEFORE its implementation exists.

**Timing disclosure (§9.1–9.2).** At drafting time no CWI code exists. The A2 artifacts (committed) are known and inform the registered comparisons; all H9/H10 predictions about CWI behavior are forward-looking. Registered comparators are the frozen A2 artifacts rebuilt in the same run.

**A3.1 New system: `cwi` — incremental Conflict Witness Index** (plan §5; the implementation of the formal doc's §"Proposed method"). Pure TS, no store, no reasoner: (i) per-(keyed class, key property, key value) buckets with **incremental union–find** over co-key membership (~K maintained under insertion, union-by-size); (ii) per-equivalence-class, per-E-predicate entry lists carrying each supporting record's τ/σ (record-level annotations, as in A2.1.3); (iii) **incremental conflict emission/retraction** at equivalence-class granularity: any insert (type, key value, E-value, or late-arriving τ/σ annotation) re-evaluates only the affected class × predicate cell — per-insert work is bounded by affected-class size, never store size; late τ/σ annotations may RETRACT previously-emitted conflicts (τ = ⊥ overlaps everything, so annotations only narrow); (iv) **minimal-witness assembly at query time**: shortest record–bucket–record path between the two supporting records (BFS within the class), yielding |W| = 3m+3 cross-record witnesses (types of path records + 2 key assertions per link + 2 value assertions + the two records' τ/σ annotation triples where present) and |W| = 2 (+ annotations) same-record witnesses. The θ-gate is vacuous on the fixtures (every assertion carries confidence ≥ 0.5 after `seedProvenance`), matching the exact arms.

**A3.2 Retrieval contract and the three variants (Def. 3.3 Remark 1; clause-3 second conjunct).** `query(seed)` returns the conflicts on the seed's equivalence class with their witnesses. Returned context per variant: **`cwi-witness`** = the witness triples serialized (sorted N-Triples convention, µ_byte over that serialization); **`cwi-pointer`** = the conflict tuple + witness triple *ids* only (premises NOT in context; machine-checkable only after dereference); **`cwi-flag`** = the conflict tuple (entity, predicate, values) with no witness (witness field empty; witnessRecall 0 by construction). All three share `flagged`/`values`; they differ in `witness` and context bytes. Worst-case-seed semantics as in the retrieval arms: `flagged` requires every subject's query to return the matching conflict.

**A3.3 Outputs and scoring.** `src/cwi/` (index + runner CLI); `results/cwi/cwi.<domain>.jsonl` — PredictionRows for the three systems, `extra` = `{variant, contextTriples, contextBytes, queryUs, ingestMs, insertWrites, indexEntries, sourceTriples}`; `results/cwi/ledger.<domain>.json` — the maintenance ledger: total ingest ms, **update amplification** (index writes per source assertion), index size, query latency p50/p95. Scored by the unchanged `score-cli`; H9/H10 verdicts added to `phase1-verdicts-cli.ts` (hashes recorded at the landing commit, as before). Build integration: new arm in `scripts/build-evidence.sh` stage `deterministic`, files inventoried by the summary.

**A3.4 Hypotheses (directional, registered before implementation).**

- **H9 (witness-sized completeness, m-independent).** `cwi-witness` achieves conflict P = R = 1.0 and witnessRecall = 1.0 on ALL eight domains (including chains and τ/σ, where A2 showed the plain join and the reasoner respectively fail), with mean returned context in triples EXACTLY the mean gold |W| per domain, and **strictly smaller than the smallest witness-complete key-aware ball** on every cross-record domain (key-aware needs k ≥ m, and its ball grows with k — H6.iii — while the witness does not). Detection-set equality with `exact-key-join-x` on every domain is a test-enforced cross-engine check (plan Phase 1 "verify against an independent implementation").
- **H10 (maintenance ledger, direction only; single-run wall-clock disclosed run-variable).** (i) Update amplification is scale-flat: mean index writes per source assertion on `conflict-xr-scale` within 2× of `conflict-xr-small`; (ii) per-query latency p50 < 1 ms on every domain and does not grow with chain length m (m3 p50 within 2× of m2 p50); (iii) ingest-once + query-all-instances total cost stays within one order of magnitude of one-shot `exact-key-join-x` end-to-end on every domain (the price of incrementality is bounded).

**A3.5 Clause-3 decision rule, pre-committed.** Falsification clause 3's second conjunct asks whether "store-side detection plus a flag-pointer satisfies consumers within budget," making full-witness retrieval unnecessary. Adjudication rule fixed NOW: compute the byte overhead ratio `mean(contextBytes cwi-witness) / mean(contextBytes cwi-pointer)` over conflict instances per domain. If the ratio is ≤ 5× on every domain, the strict Def. 3.3 form (whole witness in context, machine-checkable without dereference) costs at most a small constant over the pointer form and is adopted as the paper's headline contract, with the pointer/flag variants reported as ablations; if the ratio exceeds 5× anywhere, the Remark-1 weaker variants are promoted to co-headline and clause 3 is reported as PARTIALLY armed. No hypothesis is registered on this ratio (either outcome is informative); only the decision rule is frozen.

**A3.6 Non-claims.** No claim that CWI beats the exact baselines at store-side detection (clause-3 first conjunct stands as measured in A2). No novelty claim for union–find or incremental joins (Prop. 4 / plan §5 already concede this); the claimed contribution remains the conflict-completeness framing plus the measured completeness-per-budget and maintenance ledgers of which CWI is the witness-indexing point. Synthetic-fixture scope: external validity is deferred to Phase 2, exactly as in A2.

### Amendment A4 — 2026-07-13 (B-bounded completeness curves; load-scale ledger strata; τ/σ-aware reasoner system; C1 annotation harness)

**Timing disclosure (§9.1–9.2).** Drafted after the A2/A3 results landed (all known) and BEFORE any of the code registered below exists. H11/H12 are forward-looking; the curve script computes an already-registered primary metric (§4.3) from already-frozen prediction rows.

**A4.1 B-bounded completeness curves (§4.3 primary metric, now operationalized).** New scoring script `src/instances/bcurves-cli.ts` → `results/curves/bcurves.<domain>.json` (+ printed table), computed from the frozen PredictionRow files only (retrieval sweep + `cwi-witness`), over conflict instances. Grids: triples {5, 10, 25, 50, 100, 250, 500, 1000, whole-store}; bytes {1024, 2048, 4096, 8192, 16384, 32768, 65536, 131072, whole-store}. §4.3's sentence ("a policy's point at B uses only instances where its returned context ≤ B") is ambiguous against Def. 5.3's truncation reading; BOTH are emitted, and the **primary curve is the Def. 5.3 operationalization**: WCR_R(B) = fraction of ALL conflict instances that are witness-complete with returned context ≤ B (truncation beyond B cannot preserve a whole witness, by Def. 2.1 minimality). The §4.3-literal conditional rate (denominator = instances with context ≤ B, reported with its n) is emitted alongside as `conditionalRate`. This interpretation choice is registered HERE, before the script exists; no result had been computed under either reading at drafting time.

**A4.2 Load-scale ledger strata (plan Phase 1: "measure update/query complexity against exact baselines").** Deterministic scale generator (mulberry32, seed `0x5ca1eab1e`), v2/xr topology (20% conflicted twins, sparse session-2 records), sizes **persons ∈ {300, 1000, 3000, 10000}** (≈1.7k–56k triples). These are **cost-only strata**: NOT committed to the repo (size), regenerated at measurement time, with the sha256 of the generated canonical fixture JSON recorded in the artifact; they carry NO detection-accuracy claims and add NO instances to the benchmark. Runner → `results/scale/scale-ledger.json`: per size × system — `exact-key-join`, `exact-key-join-x`, `sparql-groupby` (end-to-end detection wall-clock), `reasoner-r14r23r22` (**sizes ≤ 1000 persons only, 300 s timeout**, timeout reported as `timeout`, never a number), and CWI (full maintenance ledger + query p50/p95 over the **first 50 conflicted persons by index** — deterministic sample, registered here). All wall-clock fields single-run and run-variable; counts and sha256 deterministic. Build integration: new stage `scale` in `scripts/build-evidence.sh` (after `verdicts`, before `summary`); the artifact is inventoried by the summary.

- **H11 (scale ledger, direction only).** (i) CWI update amplification at 10,000 persons is within 2× of its 300-person value (scale-flat); (ii) CWI query p50 < 1 ms at every size; (iii) CWI ingest total within 10× of `exact-key-join-x` end-to-end at every size. Reasoner and SPARQL growth are REPORTED (no registered direction); the known 55 ms → 6.2 s growth on mechanism-v0 makes superlinear growth expected but it is not a hypothesis. Verdict computed and stored by the scale runner inside `results/scale/scale-ledger.json` (`h11` field).

**A4.3 New system: `reasoner-tau`** — the F-fragment chain with F3's overlap side conditions implemented (τ/σ-aware), as NEW rule variants in `predicate-reasoner` (`r23t`: sameAs value propagation carrying the SOURCE record's τ/σ forward — RDF-star annotation on the inferred triple, chained propagation preserving the original annotations; `r22t`: pairwise exclusivity violation gated on τ-interval overlap and σ equality-or-absent, reading record-level annotations for asserted values and RDF-star annotations for propagated values). The frozen `reasoner-r14r23r22` arm is UNTOUCHED (its blind behavior is a registered H7 result). New arm CLI → `results/instances/reasoner-tau.<domain>.jsonl`, runs on all 8 domains, scored by the unchanged scorer.

- **H12 (τ/σ-aware reasoner).** `reasoner-tau` achieves P = R = 1.0 with zero benign flags on `conflict-tausig` (12/12 conflicts, 0/24 τ-or-σ negatives), and its flagged-instance sets are IDENTICAL to `reasoner-r14r23r22` on every τ/σ-free domain (d20, xr-small/scale, chain-m2/m3, h3-nk1/nk3) — the extension changes nothing where annotations are absent.

**A4.4 Exploratory tooling: C1 annotation harness** (domain-selection §6 C1 step 4; A2.7 continuation). Deterministic stratified sampler over the OSV probe's group classifications (strata sizes fixed in the harness guide before sampling), annotation guide, per-annotator record format, and agreement/adjudication scripts (Cohen's κ) under `papers/paper1/probes/c1-annotation/`. Exploratory: produces no confirmatory numbers under this registration; the semantic-disagreement rate it will eventually yield is a Phase-2/Gate-C input requiring its own amendment before confirmatory use.

**A4.5 Scoring-code note (§9.2).** `bcurves-cli.ts`, the scale runner, and the `reasoner-tau` arm are NEW files; no existing frozen scoring code is modified. Hashes recorded at the landing commit, as for A1–A3.

**A4.5 landing note (2026-07-13, after the runs).** New-file sha256 (first 16 hex), recorded per A4.5: `src/instances/bcurves-cli.ts` `83d954487083f7bf`; `src/scale-ledger/scale-ledger-cli.ts` `5221f9caffae02db`; `src/scale-ledger/reasoner-worker.ts` `0352dda6b084bb68`; `predicate-reasoner/src/rules/r22t-value-conflict-tau.ts` `893271b9738ddc97`; `.../r23t-sameas-value-propagation-tau.ts` `117d932966afa8f9`; `src/instances/reasoner-tau-cli.ts` `baa09ce17b9ed04a`; `probes/c1-annotation/sample-cli.mjs` `d64af6e08776a1e7`; `probes/c1-annotation/agreement.mjs` `8142420a262ec125`. The only pre-existing file changed is `predicate-reasoner/src/rules/index.ts` (two ADDITIVE export lines for r22t/r23t; the `RULES` array and every existing entry are byte-unchanged, so no arm's registered behavior moved). Outcomes observed this day: H11 PASS (amplification 1.895 at 300 persons → 1.893 at 10,000; query p50 5–8 µs throughout; CWI ingest 52.6 ms vs `exact-key-join-x` 119.7 ms at 56k triples, i.e. cheaper — the reasoner, 40.6 s already at 5.6k triples, is skipped per the ≤1000-person cap), H12 PASS (reasoner-tau P=R=1 with zero benign flags on conflict-tausig and flagged-set-identical to the blind arm on all seven τ/σ-free domains), and B-curves emitted for all eight domains with the strict Def-5.3 primary reading. The A2/A3 verdicts H3–H10 and the clause-3 adjudication reproduced unchanged.

### Amendment A5 — 2026-07-16 (C1 semantic annotation promoted to confirmatory; LLM-reader arm over retrieval contexts; hypotheses H13–H14; §8 correction of record)

**Timing disclosure (§9.1–9.2).** Drafted 2026-07-16, after every A2/A3/A4 result is known (H3–H12 all resolved in their registered directions). §9.2 requires this section to state **what had already been observed at the time**. Stating it exactly:

- **before** any code registered in A5.2 exists;
- **before** any C1 annotation label exists — no annotator has been recruited, no `sample.jsonl` has been annotated, so A5.1 registers the confirmatory use of the A4.4 harness before the adjudicated labels exist, which is the precondition A4.4 imposed on itself. **A5.1 is forward-looking in the strict sense.**
- **NOT before LLM reader outcomes have been observed.** An earlier draft of this amendment asserted that no non-dry-run LLM call had ever been made. **That assertion was false and is retracted here before landing.** It is true only of the *pinned* runner (both `results/raw/*/manifest.json` carry `dryRun: true`). But **Pilot G (2026-07-12, EXPERIMENT-LOG) made real, non-dry-run model calls** through `flat-auto` against a local OpenAI-compatible server (`openai:qwen3.5-ctx32k:latest` ≈ qwen3.5 9.7B "weak"; `openai:gemma4-ctx32k:latest` ≈ gemma4 e4b "mid"), plus in-session frontier subagents (permanently exploratory), on the `flat-all` and `flat-retrieved` (**k=2 iri-bfs ball**) arms over `conflict-xr-small` / `conflict-d20`, n=1 per cell. Its outcomes on `conflict-xr-small` (aggregate / conflict slice) are known and are reproduced here so that no reader of this registration has to take H13 as more virgin than it is:

| tier | flat-all | flat-retrieved (k=2, iri-bfs) | reasoner |
|---|---|---|---|
| weak (qwen3.5 9.7B) | 0.125 / 0.000 | 0.000 / 0.000 | 1.000 |
| mid (gemma4 e4b) | 0.375 / 0.250 | 0.250 / 0.000 | 1.000 |
| frontier (in-session) | 1.000 / 1.000 | 0.583 / 0.167 | 1.000 |

**Consequences, registered rather than glossed.** (i) H13 is **not** forward-looking for the (`flat-all`, `iri-bfs@2`) × (weak/mid/frontier) cells on `conflict-xr-small`; it is forward-looking for every other context source (`key-aware@{1,2,3}`, `literal-aware@1`, `cwi-witness`, `iri-bfs@{1,4}`), every phase1-v3 domain, and the entire A5.2.6 roster, none of which any model has ever read. (ii) More seriously, **Pilot G's weak/mid rows contradict the naive form of H13**: flat-all conflict-slice recall moves 0.000 → 0.250 → 1.000 across tiers *within one context source*, which is a larger spread than the frontier's 1.000 → 0.167 *across* context sources. A version of H13 claiming across-model variance is unconditionally smaller than across-source variance is **already falsified by data in hand**, and registering it would be registering a known-dead hypothesis. H13 is therefore stated below with a **competence gate**, and the gate's motivation (Pilot G's observed weak-tier collapse — "weak tier cannot use the KB at all (even direct recall fails)") is disclosed as post-hoc-informed. (iii) H14 is **unaffected and fully forward-looking**: Pilot G scored F1 only and never asked what a reader does when handed one side of a conflict, so no silent-selection outcome has ever been observed by anyone.

**A5.1 C1 semantic-disagreement estimate promoted to CONFIRMATORY (Gate B condition 5(c)).** A4.4 registered the annotation harness as exploratory and required "a future pre-registration amendment that registers the estimate's confirmatory use before the adjudicated labels are analyzed." This is that amendment. Nothing about the sampling frame, the strata, the seed, the guide's label set, or the tie-break policy is changed — those were fixed in `probes/c1-annotation/guide.md` before the sampler first ran, and re-opening them now would void the registration. What is added is the estimand, the independence handling, the reliability gate, and the decision rule.

**A5.1.1 Estimand.** Per stratum s, let r_s = (number of sampled groups whose adjudicated label is `semantic-contradiction`) / (number of adjudicated groups in s, excluding `unsure` and unannotated, both reported with counts). The **domain-level semantic-contradiction rate** is the population-reweighted estimate ρ = Σ_s (w_s · r_s), where w_s is stratum s's share of the probe's multi-record population as measured by `c1-osv-prevalence-probe.mjs` on the SAME dated snapshot (`Last-Modified: Mon, 13 Jul 2026 07:54:02 GMT`; 4,556 multi-record CVE groups). The `no-disagreement` control stratum enters with its measured r_s (expected ≈ 0); a materially non-zero control rate is a **guide failure**, reported as such, not silently absorbed. Uncertainty: 95% CI by stratified bootstrap over groups (10,000 resamples, seed `0xc1a5`), reported with ρ.

**A5.1.2 Lineage / independence handling (probe caveat 4, resolved here).** The C1 probe report's caveat 4 states that some group members are format conversions of a common upstream (GHSA → PYSEC) and that "per-source lineage must be modeled in Phase 2 before treating disagreements as independent observations." The annotation sample *is* that Phase-2 step, so lineage is pinned down now, before any label exists. Each sampled group receives a `lineage` field computed **deterministically from the already-drawn sample** (record id prefixes plus `upstream`/`related`/`aliases` references in the record payloads) — this is a pure function of data already fixed, it does **not** re-open or re-draw the sampling frame, and it is computed before annotation:

- `independent` — the group's members trace to ≥ 2 distinct upstream authorities;
- `converted` — every member traces to a single upstream (one advisory, reformatted).

**The primary estimate ρ is computed over `independent` groups only.** The `converted` stratum is reported separately with its own rate and n, and is **never pooled** into ρ. Rationale, registered now: a disagreement between two reformattings of one advisory is not two independent records disagreeing, and counting it would inflate exactly the quantity Gate B turns on. If the `independent` subset is too small to estimate (< 30 adjudicated groups), the estimate is reported as underpowered and 5(c) is **not** declared satisfied on it — that outcome triggers the §5 Option-2 pivot consideration exactly as a failed threshold would.

**A5.1.3 Reliability gate (pre-committed).** Cohen's κ over the two annotators' labels on the full sample is reported unconditionally. **If κ < 0.60, the estimate is reported but 5(c) is NOT declared satisfied on it**: sub-moderate agreement means the label set, not the domain, is what is being measured. The remedy in that case is a guide revision plus re-annotation on a fresh sample under a further amendment — never a post-hoc relabel of the existing sample, and never a quiet drop of the disagreeing groups. Adjudication (third pass over `adjudication.csv`) resolves A/B disagreements and produces the labels ρ is computed from; the adjudicator's own rate is not a separate estimate.

**A5.1.4 Decision rule for Gate B condition 5(c) — FIXED 2026-07-16, before any label exists.** Researcher decision (Midhun Xavier, recorded from an interactive session confirmation, as for the Gate A decision):

> **Gate B condition 5(c) is SATISFIED iff all three hold:**
> 1. **ρ ≥ 0.10** — the population-reweighted semantic-contradiction rate over `independent` groups (A5.1.1–A5.1.2);
> 2. **95% CI lower bound ≥ 0.05** — stratified bootstrap per A5.1.1;
> 3. **κ ≥ 0.60** — the A5.1.3 reliability gate.
>
> Any one failing ⇒ 5(c) NOT satisfied ⇒ the §5 Option-2 pivot (NeurIPS E&D benchmark/audit paper) is triggered per the Gate A decision's own terms.

Rationale, recorded now so it cannot be reconstructed favorably later: at ρ = 0.10 the slice carries on the order of 450 genuine, independently authored, cross-record contradictions — the store shape of Prop. 3 occurring at material rates in one ecosystem. That is precisely what falsification clause 1 asks ("P3 is a theorem about a store shape that does not occur"); the paper's claim is **occurrence plus structural unreachability by IRI-adjacency retrieval**, not dominance, so a bar demanding the mechanism be *typical* would test a claim we do not make. The anticipated reviewer objection — "10% is low" — is answered by that scope sentence and by §4.4's refusal to convert prevalence into a benchmark density. These values are now frozen: §9.2's amendment-only rule applies, and a threshold moved after labels are seen voids the confirmatory status of the estimate entirely.

**A5.1.5 Non-claims (A5.1).** ρ estimates the semantic-contradiction rate among *syntactically disagreeing, multi-record, independently authored* CVE groups in one dated PyPI OSV slice. It is **not** a benchmark density, **not** a claim about other ecosystems or other domains (C2/C3 are separately registered work), and **not** a claim that the annotated groups are witness-groundable in the Def-2.1 sense (both sides stored as assertions under an oracle schema) — that is a Phase-2 construction question. No benchmark instance is mined from the sample under this amendment; the held-out hygiene of A2.7 stands. The syntactic 48.4% / 13.3% figures remain upper bounds and are not restated as semantic rates anywhere.

**A5.2 New system family: `reader:<context-source>` — the LLM-reader arm (registered BEFORE implementation).** The arm answers the question §1 and §8 of the paper draft pose: *does an LLM reading exactly what a retrieval policy returned detect the conflict, and if not, what does it do instead?* It measures the **interface**, not model intelligence (A5.6).

**A5.2.1 Unit and contract.** The unit is the **instance** (§4.3; `InstanceRecord`/`PredictionRow` per `src/instances/types.ts`), never the question or the seed. One `PredictionRow` per (instance × context source × model × run). The **frozen scorer `score-cli.ts` is reused unchanged**; the reader arm introduces no metric.

**A5.2.2 Context sources (8, registered exhaustively).** The reader is handed **exactly the context the frozen policy returned** — no new retrieval logic is written. Contexts are re-derived by the existing `rigs/retrieval-policies.ts` and `src/cwi/` code paths, using `evaluateInstance`'s **worst-seed semantics**: the reader sees the context from the seed reported in the frozen arm's `extra.seededOn`, plus the schema text (`world.ttl`), exactly as `flat-retrieved` composes context today. The eight sources:

`iri-bfs@1` (the 0-recall floor) · `iri-bfs@4` (complete-only-by-returning-the-store) · `key-aware@1` · `key-aware@2` · `key-aware@3` (the chain-depth flip at k=m) · `literal-aware@1` (the non-key-literal premium point) · `cwi-witness` (the witness point) · `flat-all` (whole-store **positive control**).

**A5.2.3 Domains and cell count.** Seven domains: `conflict-d20`, `conflict-xr-small`, `conflict-chain-m2`, `conflict-chain-m3`, `conflict-h3-nk1`, `conflict-h3-nk3`, `conflict-tausig` — **188 instances (72 conflict, 116 benign)**. `conflict-xr-scale` is **excluded from the reader arm and the exclusion is registered here**: its mechanism is byte-identical to `xr-small`'s (it is the 300-person cost twin), and its whole-store contexts (`flat-all`, `iri-bfs@4`) would dominate the arm's token cost while testing nothing `xr-small` does not. This exclusion carries no detection claim; `xr-scale` remains in every non-reader arm unchanged.

**A5.2.4 Prompts (two per instance, fixed now).** Q1 **detection**, on all 188 instances × 8 sources: the context, then a boolean-plus-values question about the instance's entity and predicate → `PredictionRow{flagged, values, witness: []}`. Q2 **silent-selection probe**, on the **72 conflict instances only** × 8 sources: an open question ("what is ⟨entity⟩'s ⟨predicate⟩ according to this context?") whose instruction explicitly permits three outcomes — name a single value, report a conflict, or abstain for insufficient information. Q2's response is classified into `{single-value, conflict-reported, abstain, unparseable}` by a deterministic parser. Both prompt templates are frozen at the landing commit and recorded by sha256 (the existing `promptSha256` manifest field already does this per task).

**A5.2.5 Repeats, decoding, and §9 rule 4.** Per §9 rule 4 ("the number of repeats for stochastic LLM arms is fixed before the first repeat of that cell runs"): **runs = 3** per (instance × source × model), fixed now and not extendable for any reason, including an unfavorable interim estimate. Decoding: `temperature = 0`, `seed = run index (1..3)`, `max_tokens = 1024`. Cell count per model: Q1 188 × 8 × 3 = 4,512; Q2 72 × 8 × 3 = 1,728; **6,240 completions per model**. Parse failures are counted as **misses, never dropped** (§6, unchanged), and `unparseable` is a reported Q2 outcome, not an exclusion.

**A5.2.6 Model roster — FIXED 2026-07-16, before the first non-dry-run call.** H13 compares across-model against across-context-source variance and is **untestable with a single model**; the researcher's decision was a three-lineage roster. Frozen roster (three independently pretrained families, one gateway):

| Spec | Lab | Verified 2026-07-16 |
|---|---|---|
| `openai:z-ai/glm-5.2@a` | Z-AI | HTTP 200, clean minimal JSON (92 tok) |
| `openai:deepseek-ai/deepseek-v4-flash@b` | DeepSeek | HTTP 200, clean minimal JSON (38 tok) |
| `openai:qwen/qwen3.5-122b-a10b@c` | Qwen / Alibaba | HTTP 200, clean minimal JSON (25 tok) |

**Substitution of record.** The roster the researcher selected named `deepseek-v4-pro` and `mistral-large-3-675b-instruct-2512`. Neither is servable on the available credential and **the selected roster could not be registered verbatim**: `deepseek-v4-pro` and every Mistral instruct model in the catalog (`mistral-large-3-675b`, `mistral-large-2`, `mistral-medium-3.5`, `mixtral-8x22b`) return either HTTP 404 `Not found for account` or a 90 s zero-byte timeout, with and without `stream:true`. The catalog `/models` advertises 118 ids; entitlement is a strict subset, so **catalog presence is not availability** and every registered spec above was individually verified by a live completion before being written here. The substitution preserves the decision's actual content — three independent pretraining lineages for the H13 variance estimate — retaining DeepSeek at its `flash` tier and replacing the unreachable Mistral with Qwen3.5-122B. Also verified reachable but **not** registered, recorded so the roster cannot be seen as fished after the fact: `google/gemma-4-31b-it`, `meta/llama-3.3-70b-instruct` (both clean), `minimaxai/minimax-m3` (clean but verbose, 176 tok), `nvidia/nemotron-3-super-120b-a12b` (**leaks chain-of-thought into `content`**, hostile to a strict JSON parser — excluded on that ground, stated now rather than after seeing its accuracy).

**Tier labels `@a/@b/@c` are deliberately non-semantic.** The arm makes no weak/mid/frontier capability claim (A5.6), so the roster carries no capability ordering that a reader could mistake for one.

Every model is addressed through the existing pinned path (`PinnedProvider` verbatim raw logging of each attempt, retry/backoff, run manifest). Registered operational note: this gateway returned transient `503 ResourceExhausted` during roster verification (`deepseek-v4-flash` failed once, then succeeded unmodified). 503 is 5xx, so the frozen `fetchWithRetry` backoff already covers it; a cell exhausting all retries is a **parse failure counted as a miss** (§6), never a silent drop and never a re-run of that cell alone.

**A5.2.7 Reproducibility status — a disclosed asymmetry.** Reader-arm numbers are **not byte-reproducible** and are registered as such. Hosted endpoints are not version-pinned by their vendors: a spec such as `z-ai/glm-5.2` served via an OpenAI-compatible gateway names a *tag*, not a snapshot, and the provider may move it without notice. Reproducibility for this arm therefore rests on the **archived raw request/response logs** (`results/raw/<runId>/`, every attempt verbatim with usage and request ids) plus the run manifest, not on re-execution. Consequences, registered:

- the reader arm is **NOT** added to `build-evidence.sh`'s `deterministic` stage and its outputs are **excluded from the byte-drift check**; it gets its own stage `reader`, gated on API-key presence and **skipped with a recorded note** when keys are absent, so a clean checkout without keys still reproduces every other decision value exactly (§5.1's one-command claim is preserved verbatim);
- no reader number may head a table whose other rows are deterministic without carrying this caveat;
- the arm is **re-runnable, not replayable**; a later re-run against a moved tag is a new run with a new runId, never an overwrite.

**A5.2.8 Outputs.** `results/reader/reader.<domain>.jsonl` (PredictionRows, `extra` = `{contextSource, policy, hops, model, tier, run, contextTriples, contextBytes, promptTokens, completionTokens, q2Outcome, rawRef}`) and `results/reader/reader-summary.json` (per model × source: P/R/F1, benign FPs, silent-selection rate, parse-failure count, token totals). **`witnessRecall` is not defined for this arm and is not emitted**: a reader cites no triple ids, so the witness column would be vacuously 0 and must not be read as a completeness failure.

**A5.3 Hypotheses H13–H14 (directional, registered before implementation).**

**H13 — among competent readers, reader recall tracks retrieval witness-completeness, not model strength.**

**H13a (the ceiling — unconditional, holds for every model including incompetent ones).** For every model M and context source R, the reader's recall on conflict instances is bounded by R's witness-completeness rate up to parse noise: `recall(M, R) ≤ WCR(R) + ε`, ε = 0.05. This is the structural claim and it admits **no** competence escape: a model cannot detect what it was not shown, so a *weak* model does not get to exceed the ceiling and a strong one does not either. Registered directional consequences, per the frozen Table-2 WCR values: recall ≈ 0 on `iri-bfs@1` for every domain and on `iri-bfs@{1,2,3,4}` for `chain-m2`/`chain-m3` (no hop budget reaches the sparse intermediates); recall ≈ 1 on `cwi-witness` for all seven domains, for every competent model; `key-aware` recall flips from ≈ 0 to ≈ 1 exactly at k = m on the chain families. **Any** cell with `recall(M, R) > WCR(R) + 0.05` falsifies H13a and indicates leakage (the model answering from pretraining or from the schema rather than the returned context) — which would be a finding about the fixtures, reported as such.

**H13b (the competence-gated variance claim).** Define a model M as a **competent reader** iff its recall on the `flat-all` positive control is ≥ 0.80 on conflict instances — i.e. handed the whole store, it finds the conflict. **Restricted to competent readers**, the variance of `recall(M, R)` across models M *within* a context source R is strictly smaller than its variance across context sources R *within* a model M. Models failing the gate are reported in full, with their flat-all control recall, and excluded from the variance comparison only.

**The gate is post-hoc-informed and that is disclosed, not hidden.** Pilot G observed weak (0.000) and mid (0.250) tiers collapsing on the flat-all control while the frontier saturated at 1.000, and the un-gated form of H13b is already falsified by exactly those rows (timing disclosure above). The gate is registered now, before the A5.2.6 roster reads anything, on a principled ground stated in advance: **this arm measures the interface, and a model that cannot find a conflict when handed the entire store is not measuring the interface — it is measuring itself.** The `flat-all` control exists precisely to make that distinction observable rather than assumed, and it was in A5.2.2's registered source list before this hypothesis was written. Two guards against the gate becoming a fishing licence: the 0.80 value is frozen here and is **not** tunable after the fact; and if **no** roster model clears the gate, H13b is **untestable** and is reported as untestable — never rescued by lowering the gate.

**Falsification.** If H13b fails among competent readers — if which model you use matters more than what the policy returned — H13 is FALSIFIED and §1's "the locus of the failure is retrieval, not reasoning" framing loses its confirmatory support and must be restated as exploratory. Note the asymmetry, registered deliberately: **H13a failing would be worse than H13b failing.** H13a is the paper's actual structural claim; H13b is a claim about how much model choice matters *given* the structure, and its failure would weaken the framing without touching Prop. 3 or Prop. 5.

**H14 — silent selection on witness-incomplete contexts.** Restrict to conflict instances where the context source is witness-INcomplete but *does* contain exactly one side of the conflict (the concrete agent-memory failure §1 describes: the retriever surfaced one record, not both). On these, the reader's Q2 `single-value` rate — naming one value with neither a conflict report nor an abstention — is **strictly greater** than its `single-value` rate on witness-complete contexts of the same instances, for every model in the roster. This is the paper's motivating claim ("an agent that silently selects the stale office … with no signal that a contradiction was ever present") made confirmatory rather than exploratory. If instead readers abstain or hedge at comparable rates on incomplete contexts, H14 is FALSIFIED: the models are self-protective under partial context, the "silent" in silent selection is unearned, and §1's framing must be weakened to say the *warrant* is absent rather than that the agent is *confidently wrong*.

Both hypotheses are registered as directional; either outcome is reported as measured, in the registered direction or against it, per §9.2.

**A5.4 Correction of record — paper.md §8's reader-arm status.** paper.md §8 currently reads: "A pinned frontier-model track (does an LLM reading each policy's returned context detect the conflict?) is built and dry-run-verified but unexecuted pending API access." **This overstates what exists** and is corrected in the same commit as this amendment, in the manner of A2.5. The accurate statement:

- **Built and dry-run-verified:** the pinned-runner *infrastructure* — `PinnedProvider` (fetch-level verbatim raw logging of every attempt, including retries and network failures), `MockProvider` (canned deterministic zero-network transport), retry/backoff, run manifest with fixture and prompt hashes — plus **two question-level arms**: `flat-all` (whole-KB context) and `flat-retrieved` (**the `iri-bfs` ball at k hops — one policy, not "each policy"**), over the **legacy 8-question `eval` task set**, on **mechanism-v0 domains only**, emitting `PinnedCellRow` (question granularity, `flatF1`), which **does not feed the instance scorer or the B-curves**.
- **Not built:** a reader over *each policy's* returned context (`key-aware`, `literal-aware`, `cwi-witness` have **no** reader path), at **instance** granularity, on the **phase1-v3** domains. That arm is registered as new in A5.2.
- **Never executed *through the pinned path*:** not merely "unexecuted pending API access" — **no non-dry-run pinned call has ever been made**; both archived run manifests carry `dryRun: true`. This is **not** the same as "no LLM has read a retrieval context": Pilot G (2026-07-12) did exactly that through the *non-pinned* `flat-auto` path against a local server, on `flat-all` and `flat-retrieved` (k=2 iri-bfs), with the outcomes reproduced in this amendment's timing disclosure. §8 must not be corrected into a second, subtler overstatement in the other direction — the honest status is *infrastructure built and dry-run-verified; one policy read by local models at question granularity in an exploratory pilot; the registered instance-level per-policy arm not built and never run*.

§8's sentence is replaced by one stating the built infrastructure, the not-built arm, and the registered A5.2 plan. No §5–§6 number moves: the reader arm has never contributed a number to any table.

**A5.5 Scoring-code and build-integration note (§9.2).** The reader arm is entirely NEW files (`src/reader/`); **no existing frozen scoring code is modified**, and `score-cli.ts`, the retrieval policies, and `src/cwi/` are read-only dependencies — if any of them changes, this amendment is void and a further one is required. `build-evidence.sh` gains one stage (`reader`) after `scale`, key-gated and skippable per A5.2.7; every existing stage is byte-unchanged. New-file sha256 recorded at the landing commit, as for A1–A4.

**A5.6 Non-claims (A5.2/A5.3).** *(A5.2.6's roster is superseded by Amendment A6; A5.6's non-claims stand unchanged and extend to every model A6 adds.)* The reader arm makes **no claim that any model is more or less capable than another** — the roster exists solely to break the single-model confound in H13, and a model's absolute accuracy is not a result of this paper. It makes **no claim about frontier capability**, no claim that these readers behave like a deployed agent, and no claim that reader accuracy on synthetic oracle-schema fixtures transfers to production memory systems (§4.4/§8 scope stands unchanged). H13 concerns the *interface*: the property under test is what the policy returned, and the reader is the instrument that makes "returned enough for a sound detector" (Def. 2.3) operational for a *non-symbolic* consumer. A reader that fails on a witness-complete context is a reader failure and is reported as one — it does not impeach the policy.

### Amendment A6 — 2026-07-16 (reader roster: GLM-5.2 removed on throughput grounds; Gemma-4 and Llama-3.3 added; four lineages)

**Timing disclosure (§9.1–9.2).** Drafted 2026-07-16, after the A5.2 arm began running and therefore **after reader outcomes have been observed**. §9.2 requires this section to state exactly what had been observed at the time. Stating it:

- **269 cells** were written (2.0% of the A5 arm) before the run was stopped for this amendment: 113 `deepseek-v4-flash` and 143 `qwen3.5-122b` cells on `conflict-d20`, plus **13 `glm-5.2` cells** (6 on `conflict-d20`, 7 on `conflict-h3-nk1`).
- `conflict-d20` is the **same-record** family, where every policy is witness-complete and, per the paper's own §6, the machinery earns least. No separation between context sources is measurable there, and none was read.
- **The 13 removed-model cells are disclosed in full**, because a roster change made after seeing a model's output is exactly the manoeuvre a registration exists to constrain. GLM-5.2 was **performing well** on them: it flagged correctly on all 12 witness-complete contexts, and on the one witness-**in**complete context (`h3-nk1`, `iri-bfs@1`) it correctly did not flag and named a single value. **The model being removed was the one behaving best.** Its removal therefore cannot be, and is not, results-driven.
- No hypothesis verdict (H13a, H13b, H14) was computed at any point before this amendment, and none is computable from `conflict-d20` alone.

**A6.1 Roster change and its sole ground.** A5.2.6's frozen roster (`glm-5.2`, `deepseek-v4-flash`, `qwen3.5-122b`) is superseded by:

| Spec | Lab | Status 2026-07-16 |
|---|---|---|
| `openai:deepseek-ai/deepseek-v4-flash@b` | DeepSeek | registered in A5.2.6, **retained**, 113 cells already collected |
| `openai:qwen/qwen3.5-122b-a10b@c` | Qwen / Alibaba | registered in A5.2.6, **retained**, 143 cells already collected |
| `openai:google/gemma-4-31b-it@d` | Google | **added** — HTTP 200, clean minimal JSON at roster verification |
| `openai:meta/llama-3.3-70b-instruct@e` | Meta | **added** — HTTP 200, clean minimal JSON, 14 s re-verified today |
| ~~`openai:z-ai/glm-5.2@a`~~ | Z-AI | **REMOVED — throughput only** |

**The ground for removing GLM-5.2 is throughput and nothing else.** Measured over a 30-minute window at 4-way shard concurrency: `qwen` 63 cells, `deepseek` 60 cells, **`glm-5.2` 6 cells** — an order of magnitude slower, implying ≈ 16 days for its 4,512 cells against ≈ 1.5 days for the others. Every 95–112 s latency recorded during A5.2.7's verification was a GLM-5.2 call; the gateway is not uniformly slow, that model's NVCF worker pool is. A roster whose completion time is dominated 10:1 by one member is an operational defect, not a finding about conflict completeness.

**A6.2 Why this is not a post-hoc search for a favourable model.** Both incoming models were **named in A5.2.6 before any reader datum existed**, in the sentence recording candidates "verified reachable but **not** registered … recorded so the roster cannot be seen as fished after the fact." This amendment draws from that pre-existing, pre-data list rather than searching the catalogue now. The one other model on that list, `nvidia/nemotron-3-super-120b-a12b`, remains excluded for the reason given there (it leaks chain-of-thought into `content`), and that exclusion is not revisited. `minimaxai/minimax-m3` also remains unregistered.

**A6.3 Disposition of the removed model's cells.** The 13 `glm-5.2` cells are **deleted**, not retained and not analysed: a de-registered model's partial rows must not sit in `results/` where a later reader could mistake them for an arm. Their raw request/response logs are **retained** as the operational record substantiating A6.1's throughput measurement. The 256 `deepseek`/`qwen` cells **stand** — those models are unchanged and still registered, their cells were produced under the frozen prompts and contexts, and nothing about this amendment touches them.

**A6.4 Cell count (supersedes A5.2.5's arithmetic; everything else in A5.2.5 stands).** Repeats remain **runs = 3**, fixed and non-extendable per §9 rule 4 — this amendment adds *new cells* (new model × instance × source), it does not extend the repeats of any cell that has already run, and every added cell is registered here before it runs. Per model: Q1 188 × 8 × 3 = 4,512; Q2 72 × 8 × 3 = 1,728; **6,240 completions per model**. Arm total: **4 models × 4,512 = 18,048 cells / 24,960 completions**.

**A6.5 Effect on H13b.** The competence gate is **unchanged**: `flat-all` conflict recall ≥ 0.80 to enter the variance comparison, frozen in A5.3 and not revisited here. Four lineages (DeepSeek, Qwen, Google, Meta) give the across-model variance four points rather than three. A5.2.7's single-gateway caveat **stands and is not weakened**: all four models are served by one NVCF gateway, so the arm still cannot separate model from serving stack, and no claim depending on that separation may be made.

**A6.6 Registered operational note: NVCF per-model latency is volatile.** `gemma-4-31b-it` returned in ~1 s at roster verification and exceeded 90 s on two probes three hours later; `glm-5.2` was fast at 12:0x and ~100 s by 12:3x. Per-model queue depth on this shared pool moves unpredictably, so **any** roster member may become a long pole mid-run, `gemma-4` included. This is recorded now so that a slow model later is a known condition rather than a surprise. It changes no registered quantity: the runner leaves transport-failed cells **unwritten** rather than zero-filling them (a throttled call cannot fabricate a miss), the driver is resumable, and an arm that ends incomplete is **reported as incomplete** — never silently short. Should `gemma-4` prove as dominating as GLM-5.2 was, the honest options are to let it trickle or to remove it by a further amendment on the same throughput-only ground; its cells would then be deleted exactly as GLM-5.2's were.

### Amendment A7 — 2026-07-16 (reader roster: Llama-3.3 removed on throughput; MiniMax-M3 and Nemotron-3-Super added; two A5.2.6 exclusion grounds retracted as probe artifacts; five lineages)

**Timing disclosure (§9.1–9.2).** Drafted 2026-07-16, after the A6 arm had run 25 minutes. Observed at the time: **622 cells (3.4% of the A6 arm), every one on `conflict-d20`** — the same-record family where every policy is witness-complete and, per the paper's §6, the machinery earns least. No chain, h3, or tausig cell exists for any model. No H13a/H13b/H14 verdict was computed and none is computable from `d20` alone.

**A7.1 Roster.** A6.1's four-model roster is superseded by **five**:

| Spec | Lab | Change |
|---|---|---|
| `openai:deepseek-ai/deepseek-v4-flash@b` | DeepSeek | retained (175 cells stand) |
| `openai:qwen/qwen3.5-122b-a10b@c` | Qwen / Alibaba | retained (258 cells stand) |
| `openai:google/gemma-4-31b-it@d` | Google | retained (162 cells stand) |
| `openai:minimaxai/minimax-m3@f` | MiniMax | **added** |
| `openai:nvidia/nemotron-3-super-120b-a12b@g` | NVIDIA | **added** |
| ~~`openai:meta/llama-3.3-70b-instruct@e`~~ | Meta | **removed — throughput only** |

Five independent pretraining lineages (DeepSeek, Qwen, Google, MiniMax, NVIDIA); H13b's across-model variance gains a fifth point. A5.2.7's single-gateway caveat **stands undiminished** — all five share one NVCF gateway, so the arm still cannot separate model from serving stack.

**A7.2 Llama-3.3 removal: ground, and an explicit weakness in that ground.** Measured over 25 minutes of the A6 run: `gemma-4` 314 cells/h, `qwen` 223, `deepseek` 102, **`llama-3.3` 46.5** — the arm's binding constraint at ≈ 4 days against 0.6–1.8 for the others. **This ground is weaker than A6's and is recorded as weaker.** GLM-5.2 was removed at ~16 days, dominating the arm 10:1 — an unambiguous operational defect. Llama at ~4 days is the long pole but does not dominate, and would likely have accelerated as faster shards freed slots. **This is the second throughput-based removal, and a pattern of swapping models until the arm runs fast is a real hazard to this registration's credibility** — one this amendment names rather than lets accrete silently. **No further model may be removed on throughput grounds without a threshold fixed in advance**; the honest reading is that A7.2 spends credibility that A6.1 had earned, and the roster is closed to further speed-motivated change.

**Llama-3.3 was performing perfectly when removed: 26 of 26 cells consistent with what it was shown** (flagged iff the context contained the full witness), zero mismatches. As with GLM-5.2, the model removed was one of the best-behaved, so the removal cannot be results-driven. Its 27 cells are **deleted** per A6.3's rule (a de-registered model's partial rows must not sit in `results/`); its raw logs are retained as the record substantiating the throughput measurement.

**A7.3 Retraction of two A5.2.6 exclusion grounds (correction of record).** A5.2.6 excluded `nemotron-3-super-120b-a12b` for "leak[ing] chain-of-thought into `content`, hostile to a strict JSON parser," and left `minimax-m3` unregistered as "clean but verbose, 176 tok." A6.2 further stated the nemotron exclusion "is not revisited." **Both grounds are retracted: they were artifacts of the probe, not properties of the models.** Both were measured with a bare one-line prompt and **no system message**. Re-tested against the **actual registered A5.2.4 prompts** with the `READER_SYSTEM` instruction:

- `nemotron-3-super`: Q1 `{"answer": true, "values": [...]}`, Q2 `{"value": "<officeA>"}` — **`content` is pure JSON**; its reasoning goes to the separate `reasoning_content` field and never reaches the parser. Both parse.
- `minimax-m3`: Q1 11 completion tokens, Q2 9 — **not verbose under the registered prompt**. Both parse.

A registration that excludes a model must exclude it for a property the model actually has under the conditions the arm will use. These two did not, and the error was mine. Registering them now corrects that rather than expanding the roster opportunistically.

**Disclosure on the format re-test.** That test showed each candidate's answer to a smoke prompt: `nemotron` answered the conflict question **correctly** (`true`); `minimax` answered it **incorrectly** (`false` on a 6-fact store containing a plain conflict). **Both are registered regardless**, so the selection cannot be favorable — one candidate looked good and one looked bad, and neither fact was allowed to decide. These were two throwaway prompts, not fixture instances; they are not data, and no verdict rests on them. Should `minimax-m3` fail the A5.3 `flat-all` competence gate (≥ 0.80), it is excluded from H13b's variance comparison **by the gate registered before any of this**, reported in full, and not removed from the roster.

**A7.4 Retraction of the "not servable" inference (correction of record).** A5.2.6 and A6.1 state that `deepseek-v4-pro` and every Mistral instruct model are "not servable on the available credential." **That inference is unsound and is withdrawn.** It rested on 90-second zero-byte timeouts — and A6.6's own volatility finding, now confirmed in the strongest form, shows timeouts are transient queueing, not unavailability: `gemma-4` timed out past 90 s on two probes and is now **the fastest model in the roster**, while `llama-3.3` answered a probe in 14 s and became the slowest. **Spot probes have no predictive value for throughput.** Only the HTTP 404 `Not found for account` results (`gemma-3-12b`, `mistral-large-2`, `mixtral-8x22b`, `nemotron-70b`, `phi-3.5-moe`, `kimi-k2.6`) are sound exclusions, being deterministic entitlement errors. The Mistral family and `deepseek-v4-pro` are therefore of **unknown**, not negative, availability. They remain unregistered — **not because they are unavailable, but because the roster is now closed**: re-opening it to search for further candidates after 622 cells have been observed would be fishing, and five lineages already exceed what H13b requires.

**A7.5 Cell count (supersedes A6.4; everything else in A5.2.5 stands).** Repeats remain **runs = 3**, fixed and non-extendable per §9 rule 4; this adds new cells and extends no cell that has run. Per model: Q1 4,512 + Q2 1,728 = **6,240 completions**. Arm total: **5 models × 4,512 = 22,560 cells / 31,200 completions**.

### Amendment A8 — 2026-07-19 (Qwen3.5-122B decommissioned by the vendor mid-run; arm permanently incomplete for that model; A5.2.7's reproducibility warning realized)

**Timing disclosure (§9.1–9.2).** Drafted after 13,874 cells (61.5% of the A7 arm) across all seven domains. Partial statistics have been computed and inspected (see the 2026-07-18 working notes: context-size recall decay, per-model collapse split, H14 silent-selection rate). **No hypothesis verdict has been declared.** This amendment records an event that **no party to this registration chose**, so its timing carries none of the discretion that made A6/A7 hazardous.

**A8.1 The event.** On **2026-07-20T00:00:00Z**, mid-run, the gateway began returning HTTP **410 Gone** for `qwen/qwen3.5-122b-a10b`:

> `{"type":"about:blank","title":"Gone","status":410,"detail":"The model 'qwen/qwen3.5-122b-a10b' has reached its end of life on 2026-07-20T00:00:00Z and is no longer available."}`

2,424 calls failed this way before the run was stopped. **The model no longer exists.** This is categorically unlike the A6/A7 removals: those were discretionary choices about slow-but-working models, and each required (and received) an argument that the choice was not results-driven. Here **no choice was available to anyone** — no credential, no pacing, no patience, and no amendment can retrieve a decommissioned model. The registration's only decisions are what to do with the cells already collected and how to report the gap.

**A8.2 Disposition of Qwen's cells — they STAND.** Qwen completed **2,088 of 4,512 cells**, with **five of seven domains COMPLETE**: `conflict-d20`, `conflict-h3-nk1`, `conflict-h3-nk3`, `conflict-xr-small`, `conflict-chain-m3`. These were produced under the frozen A5.2.4 prompts, the frozen A5.2.2 contexts, `runs = 3`, and `temperature = 0`, exactly as registered; nothing about them is provisional. They are **retained and analysed**. The A6.3/A7.2/A8.4-style deletion rule (a *de-registered* model's rows must not sit in `results/`) **does not apply**: Qwen was never de-registered — it was registered in A5.2.6, retained through A6 and A7, and remains a registered model of this arm whose data collection was terminated by a third party.

**A8.3 What is permanently missing, and how it must be reported.** `conflict-chain-m2` (984 cells) and `conflict-tausig` (1,440 cells) will **never** exist for Qwen. Per A5.2.7 ("an arm that ends incomplete is reported as incomplete — never silently short"), the registered reporting rule is fixed here:

- Every table or figure including Qwen must state its domain coverage (5/7) **at the point of use**, not in a footnote.
- **No cross-domain aggregate may pool Qwen with the other models**, because the resulting panel is unbalanced *in a way correlated with the independent variable*: the two missing domains are the τ/σ family and the deeper chain family, i.e. the hardest cases. Pooling would silently flatter Qwen. Analyses are therefore either **per-domain** (Qwen included where it has data) or **restricted to the five domains Qwen completed** (all five models, balanced) — never a pooled mean over seven domains with a hole in it.
- **H13b's competence gate is computed per-domain**, as the gate is defined on `flat-all` recall and Qwen has `flat-all` data for five domains. Its gate status is reported with its coverage.

**A8.4 Consequence for the roster and for future passes.** Qwen is removed from the *driver's* model list because every further call returns 410 — this is an operational fact, not a roster decision, and it is explicitly **not** a precedent under A7.2 (which governs removal on **throughput** grounds and remains unbroken: no model has been removed for being slow since it was written). The arm's denominator changes accordingly: four models × 4,512 = **18,048 collectable cells**, plus Qwen's 2,088 already-collected cells, for a maximum attainable **20,136 of the 22,560 originally registered**. The 2,424-cell shortfall is **permanent and is reported as such**.

**A8.5 This realizes A5.2.7's registered warning, and the paper must say so.** A5.2.7 stated, before any call was made: *"Hosted endpoints are not version-pinned by their vendors: a spec such as `z-ai/glm-5.2` served via an OpenAI-compatible gateway names a **tag**, not a snapshot, and the provider may move it without notice… the arm is **re-runnable, not replayable**."* That risk has now **materialized in the strongest possible form** — not a silently updated tag, but a model that ceased to exist four days into a five-day run. Two registered consequences follow:

1. The reader arm's reproducibility rests entirely on the **archived raw request/response logs** (`results/raw/<runId>/`), exactly as A5.2.7 committed. Qwen's 2,088 cells are backed by verbatim logs and are **not re-derivable by any other means**. Those logs are now the sole evidence for a model no one can query again, and must be preserved with the artifact.
2. The paper must report this as a **finding about evaluating on hosted models**, not merely as a mishap: an evaluation whose subjects can be withdrawn by a vendor mid-experiment cannot claim replicability in the ordinary sense, and any LLM-reader benchmark built on hosted endpoints inherits this. This is a genuine, dated, documented instance of a problem the field generally states hypothetically.
