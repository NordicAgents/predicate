\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027} % DO NOT CHANGE THIS
\usepackage[hyphens]{url} % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm} % DO NOT CHANGE THIS
\usepackage{natbib} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing % DO NOT CHANGE THIS
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{booktabs}
\pdfinfo{/TemplateVersion (2027.1)}

\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newcommand{\cwi}{\textsc{CWI}}
\newcommand{\R}{\mathcal{R}}
\newcommand{\W}{\mathcal{W}}

\title{Conflict-Complete Retrieval: Checkable Contradictions under a Context Budget}
\author{Anonymous Submission}
\affiliations{}

\begin{document}
\maketitle

\begin{abstract}
An agent cannot act on a contradiction that memory retrieval does not expose.
We define \emph{conflict completeness}: for every query-relevant conflict, a
retrieval policy returns at least one complete, source-grounded witness that a
sound checker can verify from the context alone.  We characterize the exact
budget-feasibility boundary by the minimum joint union of one witness per
conflict; when its selected union exceeds the budget, our system reports
overflow rather than silently returning incomplete evidence.  We also show
that every fixed-hop policy based only on IRI adjacency fails conflict
completeness on a literal-keyed construction.  A Conflict Witness Index
(\cwi) incrementally maintains key-induced record equivalence and returns one
shortest witness per conflict.  Controlled evaluation covers 252 instances
with 132 conflicts, plus 12 multi-conflict queries.  Shared premises reduce a
32-conflict joint certificate from 192 to 68 triples, whereas 32 disjoint
conflicts require all 192.  Exact maintained, exact on-demand, and adaptive
exact retrieval return the same 2--12 triples per individual conflict; learned
baselines require up to whole 336--440-triple stores on key chains.  All
confirmatory data are synthetic with an oracle schema: this is a retrieval
contract and cost study, not evidence of real-world conflict prevalence.
\end{abstract}

\section{Introduction}

Long-lived agents increasingly move information between a large external
memory and a bounded language-model context.  Systems such as MemGPT explicitly
manage memory tiers \citep{memgpt2023}, while graph retrieval systems such as
HippoRAG integrate information across stored passages \citep{hipporag2024}.
These systems are normally evaluated on whether retrieval supports an answer.
We ask a different question: if the store already contains incompatible
answers, what must retrieval return so the conflict is not silently hidden?

Consider two records that are declared to identify the same person through an
email key, but give different values for a single-valued predicate.  A consumer
shown only one record cannot distinguish ``no conflict exists'' from ``the
other premise was not retrieved.''  Returning a Boolean flag is also
insufficient for audit: the consumer cannot check the sources.  Returning the
whole store is complete but defeats bounded retrieval.

Our first contribution is a contract between storage and a downstream
reasoner: for each relevant conflict, return one whole minimal witness.
Minimal
explanations themselves are classical justifications, conflict sets, and
why-provenance \citep{reiter1987diagnosis,kalyanpur2007justifications,
baader2010pinpointing,buneman2001whywhere,green2007semirings}.  We do not claim
a new conflict detector or witness formalism.  What is new is to quantify
checkable-witness inclusion over the \emph{returned context}, characterize
when all relevant conflicts can fit a fixed budget, and require explicit
overflow otherwise.  Second, we prove a boundary for IRI-only neighborhoods.
Third, we implement and measure exact maintained retrieval against exact,
key-aware, and learned baselines.

The experiments produce four findings.  First, a key-aware neighborhood is a
strong baseline: for direct conflicts it is usually within a few triples of an
exact witness.  Second, chained identity is the regime where record-level
neighborhood and relevance retrieval become expensive.  Third, a maintained
index and a fresh exact join return the same witness-sized output; maintenance
does not shrink the certificate.  Fourth, joint context cost depends on premise
sharing and grows linearly in the number of disjoint conflicts.  These
boundaries are central to the claim.

\section{Conflict-Complete Retrieval}
\label{sec:model}

\paragraph{Store and schema.}
An assertion is
$a=(s,p,o,\tau,\sigma,\pi)$: an RDF-like triple, a valid-time interval,
a scope, and provenance.  A confidence threshold $\theta$ defines the premise
store $S_\theta$.  Missing confidence is excluded.  Literals use exact term
identity; no normalization or entity linker is assumed.

The oracle schema declares a single key predicate $k_C$ for a keyed class $C$
and a set $E$ of predicates that are single-valued per entity, overlapping
time, and scope.  Records $x$ and $y$ are directly co-keyed, written
$x\approx_K y$, when both have type $C$ and the same $k_C$ value.  Let
$\sim_K$ be its reflexive, symmetric, transitive closure.  Bottom time or scope
overlaps everything; otherwise time intervals must intersect and scopes must
be equal.

A conflict $\gamma=(e,p,\{v_1,v_2\},\tau,\sigma)$ exists when $p\in E$,
$v_1\ne v_2$, and two records in $[e]_{\sim_K}$ assert the values over
overlapping time and scope.  This preserve-and-surface semantics differs from
repair-based consistent query answering \citep{arenas1999cqa}: both assertions
remain in the store and the violation becomes an explicit object.

\paragraph{Rule fragment.}
The executable semantics has three rules.  First, the two type and two matching
key assertions derive $x\sim y$.  Second, equivalence propagates values only
for predicates in $E$; it is not unrestricted \texttt{sameAs} replacement.
Third, two unequal $E$-values derive a conflict exactly when their annotations
overlap.  The least fixpoint is finite: key equivalence partitions finitely
many records, propagation copies only finitely many stored $E$-values across a
partition, and violation generation is pairwise within a partition.  The
confidence gate filters leaves before any rule fires.

\begin{definition}[Witness]
A set $W\subseteq S_\theta$ is a witness for conflict $\gamma$ if the key,
type, value, time, and scope premises in $W$ entail $\gamma$, and no proper
subset does.  Let $\W_\gamma$ be all such witnesses.
\end{definition}

A same-record witness has two value assertions.  A direct cross-record witness
has two types, two matching keys, and two conflicting values, hence six
logical triples.  For a shortest chain of $m$ co-key links, intermediate types
are shared across adjacent links, giving
\begin{equation}
  |W|=(m+1)+2m+2=3m+3. \label{eq:wsize}
\end{equation}
Serialized certificates additionally carry any record-level time/scope
metadata required to reproduce the annotated assertions.

\begin{proposition}[Detector soundness and completeness]
For the rule fragment above, the least fixpoint derives a conflict if and only
if the quotient store under $\sim_K$ contains two unequal values for a
predicate in $E$ with overlapping time and scope.  Every minimal set of source
leaves in such a derivation is a witness.
\end{proposition}
\begin{proof}
For soundness, key-rule premises satisfy direct co-keying, so propagation moves
an $E$-value only between representatives of the same quotient entity.  The
violation rule then has exactly the two unequal, overlap-compatible facts
required by the conflict semantics.  For completeness, take any semantic
conflict.  Its endpoint records lie in one finite $\sim_K$ class and therefore
have a path of direct co-key steps.  Repeated propagation carries both endpoint
values to one representative, where the violation rule fires.  Removing
redundant derivation leaves until inclusion-minimal gives a witness.
\end{proof}

A query $q$ has entity seeds $\mathrm{ent}(q)$ and predicates
$\mathrm{pred}(q)$.  A conflict is relevant if its equivalence class
intersects the seeds and its predicate is requested.  Write
$\Gamma(S_\theta,q)$ for the relevant conflicts.

\begin{definition}[Conflict completeness]
A policy $\R$ is conflict-complete for $(S,q)$ iff
\begin{equation}
\forall\gamma\in\Gamma(S_\theta,q)\ \exists W\in\W_\gamma:
W\subseteq\R(q,S). \label{eq:cc}
\end{equation}
It is $(B,\mu)$-conflict-complete when Eq.~\ref{eq:cc} holds and
$\mu(\R(q,S))\le B$ for a context-cost measure $\mu$.
\end{definition}

Assume $\mu$ is monotone under set inclusion.  The least possible joint
certificate cost is
\begin{equation}
b^\star(S,q)=
\min_{\{W_\gamma\in\W_\gamma\}_{\gamma\in\Gamma}}
\mu\!\left(\bigcup_{\gamma\in\Gamma}W_\gamma\right),
\label{eq:bstar}
\end{equation}
with $b^\star=0$ when $\Gamma$ is empty.

\begin{proposition}[Budget feasibility]
\label{prop:budget}
A $(B,\mu)$-conflict-complete result exists for $(S,q)$ if and only if
$b^\star(S,q)\le B$.
\end{proposition}
\begin{proof}
If the minimum union in Eq.~\ref{eq:bstar} fits, returning that union satisfies
Eq.~\ref{eq:cc}.  Conversely, every complete result contains some witness for
each relevant conflict; monotonicity makes its cost at least $b^\star$.
\end{proof}

\begin{proposition}[Joint selection complexity]
Given explicit finite witness families and triple-count cost, deciding whether
$b^\star(S,q)\le B$ is NP-complete.
\end{proposition}
\begin{proof}
Membership is immediate.  Reduce Set Cover: create a conflict $\gamma_e$ for
each universe element $e$, a shared assertion token $a_j$ for each candidate
set $A_j$, and witness options $\{d_e,a_j\}$ whenever $e\in A_j$, where $d_e$
is conflict-specific.  A joint union costs $|\Gamma|$ plus the number of
selected set tokens, so a budget exists exactly when a cover of the
corresponding size exists.
\end{proof}

Equation~\ref{eq:cc} requires one complete proof per conflict, not every
alternative proof.  We call the latter, stronger property
\emph{proof-exhaustive completeness}.  The distinction matters because a
conflict may have exponentially many minimal proofs, while one is sufficient
for checking.  Proposition~\ref{prop:budget} also exposes an unavoidable boundary: if
$B<b^\star$, no retrieval algorithm can return complete evidence for every
relevant conflict.  It must enlarge the context, narrow the requested
predicate/seed footprint, or report overflow.

\paragraph{What the contract does and does not guarantee.}
Conflict completeness is a property of the returned assertions, independent
of the checker or language model that consumes them.  It prevents retrieval
from being the reason a finite, relevant conflict is unverifiable.  It does
not guarantee that an unsound consumer notices the conflict, that the oracle
schema is correct, or that every real inconsistency has a finite witness in
this fragment.  Conversely, a policy can satisfy ordinary answer relevance
while failing Eq.~\ref{eq:cc}; seeing one plausible value is insufficient.

\subsection{A Boundary for IRI-Only Neighborhoods}

Let $G_I(S)$ contain an undirected edge only when two IRIs co-occur as subject
and object in a triple.  Literals are not graph nodes.  A $k$-hop IRI policy
may return assertions incident to IRIs within distance $k$ of a query seed.

\begin{proposition}[Fixed-hop incompleteness]
For every finite $k$, there is a store and query with a relevant six-assertion
cross-record witness for which any policy restricted to the $k$-hop component
of $G_I(S)$ is not conflict-complete.
\end{proposition}
\begin{proof}
Create typed records $x$ and $y$ with the same \emph{literal} key $u$, and
assert different values of $p\in E$.  Seed the query at $x$.  Add no
IRI-object path from $x$ to $y$.  Then $x\sim_K y$, so the six assertions form
a relevant witness.  But $y$ is in a different component of $G_I(S)$ and its
type, key, and value assertions cannot be returned at any finite $k$.
\end{proof}

This result is deliberately narrow.  It does not apply to literal-aware,
key-aware, lexical, dense, or hybrid policies.  On connected benchmark stores,
IRI breadth-first search can also become complete by expanding to most or all
of the store.  The proposition establishes a counterexample class, not a claim
about every graph-retrieval workload.

\subsection{Conflict Witness Index}

\cwi{} maintains: (i) buckets indexed by class, key predicate, and exact key
value; (ii) union--find classes induced by bucket membership; (iii)
single-valued assertions and time/scope annotations per class; and (iv) a
record--bucket graph for certificate reconstruction.  A touching insertion or
metadata update re-evaluates the affected equivalence class.  At query time,
\cwi{} selects one shortest record--bucket path for each relevant conflict and
returns the corresponding source assertions.

\begin{proposition}[\cwi{} contract]
Under the oracle schema and exact-literal semantics, after processing a store
snapshot, \cwi{} is conflict-complete and each returned certificate is a
minimal witness.  A conflict whose shortest supporting chain has length $m$
uses $3m+3$ logical assertions, or two for a same-record conflict.  Let
$\widehat U$ be the union selected independently across $g=|\Gamma|$
conflicts.  For triple count, $|\widehat U|\le g\,b^\star$.  Budget-aware
\cwi{} returns $\widehat U$ when $|\widehat U|\le B$ and otherwise returns
\textsc{selection-overflow} with no partial context.
\end{proposition}
\begin{proof}[Proof sketch]
Union--find contains exactly the transitive closure of direct co-key buckets.
Re-evaluating every constrained predicate in a touched class enumerates
exactly pairs with unequal values and overlapping time/scope.  A shortest
record--bucket path supplies all and only the type and key premises connecting
the endpoint value assertions.  Removing an endpoint value breaks the
violation; removing a path premise breaks co-reference.  Thus the assembled
set is minimal and satisfies Eq.~\ref{eq:cc}.  Each selected shortest witness
is no larger than the corresponding witness in an optimal union, which is
itself no larger than that union; summing over $g$ gives the stated bound.
\end{proof}

Overflow does not prove that $B<b^\star$: alternative witnesses could share
more premises than \cwi's independently selected shortest paths.  It is an
explicit refusal to mislabel a truncated context as complete.  \cwi{} is an
incremental materialized view
\citep{gupta1993ivm}, specialized to retrieval of explanations.

\paragraph{Update and query path.}
On insertion, \cwi{} records a type, key, constrained value, or annotation.
A record enters a key bucket only after both its class and key are known.
Joining a non-singleton bucket unions its members and merges their constrained
value lists.  Every affected predicate cell is then re-evaluated, allowing a
later time/scope annotation to retract or restore a conflict in the current
record snapshot.  Querying a seed locates its union--find root, filters
conflicts by the query predicate footprint, and runs breadth-first search on
the record--bucket graph only to reconstruct a shortest source path.  Output
is sorted by stable triple identifier.

\paragraph{Complexity.}
Let $n_c$ be records in an affected equivalence class and $r_p$ its assertions
for constrained predicate $p$.  Bucket insertion has inverse-Ackermann
union--find overhead; merging and re-evaluating a touched class is
$O(n_c+\sum_p r_p^2)$ in the current pair-enumerating implementation.
This quadratic class-local term is a genuine worst case, hidden by the
bounded-class generator used below.  Index storage is linear in source types,
keys, constrained values, and the materialized conflicting pairs.  A query is
linear in the conflicts returned plus the record--bucket edges visited for
their shortest paths.  These bounds motivate reporting update amplification
and class-controlled scaling rather than claiming store-size-independent
maintenance.

\section{Evaluation}
\label{sec:eval}

\paragraph{Questions.}
We test: (Q1) which policies return a complete witness and at what context
cost; (Q2) whether maintained \cwi{} improves certificate size over exact
on-demand retrieval; (Q3) how maintenance and lookup costs scale; (Q4) how
joint budget cost changes with shared versus disjoint witnesses; and (Q5) how
the disclosed class-local quadratic term manifests.
All fixtures and expected labels are deterministic and synthetic.  The schema
is supplied as ground truth.  This isolates the retrieval mechanism but cannot
measure schema induction, extraction errors, or real prevalence.

\paragraph{Fixtures.}
Eight families contain 252 query instances and 132 conflicts.  D20 exercises
same-record and direct-key disagreement; XR-small and XR-scale add distracting
records; Chain-$m$ requires $m\in\{2,3\}$ co-key links; H3 varies hierarchy and
non-key predicates; and TauSig adds record-snapshot time/scope metadata.
Benign controls include shared non-key values, distinct entities, disjoint
time, and unequal scope.  Chain lengths $m=0,1,2,3$ are exercised.
Separately, twelve deterministic multi-conflict queries vary
$g\in\{1,2,4,8,16,32\}$ with either one shared identity spine or $g$ disjoint
classes.  These unique-path constructions make $b^\star$ exactly known.

\paragraph{Systems.}
\cwi{} is compared with a fresh full-store exact key/equivalence join per query
(\emph{on-demand exact}) and an \emph{adaptive exact} baseline that expands
key-aware layers and stops once it can verify a positive certificate.
Adaptive search does not generally certify absence at its configured
eight-layer cap.  Record-level policies retrieve all assertions of selected
records: IRI BFS and key-aware neighborhoods at depths 1--4; BM25
\citep{robertson2009bm25}; cosine retrieval with
\texttt{all-MiniLM-L6-v2} at a pinned model revision
\citep{reimers2019sbert}; and BM25--dense reciprocal-rank fusion with
$k_{\mathrm{RRF}}=60$ \citep{cormack2009rrf}.  Relevance methods use
top-$K\in\{1,2,4,\ldots,512\}$.

\paragraph{Protocol and implementation.}
TypeScript systems run under Node.js 22.17.1; the dense baseline uses
Sentence Transformers 5.6.1.  The encoder weights, exact revision, and Python
dependency lock are included in the artifact.  Scaling ran on one AMD Ryzen
Threadripper PRO 5955WX host with 256 GiB RAM and AlmaLinux 9.7; no GPU was
used.  Each fixture, schema, output row, and scale store has a deterministic
identifier or SHA-256 digest.  Retrieval configurations
run over identical serialized stores; the exact checker then evaluates the
context returned by each policy.  No hosted model call contributes to a
confirmatory result.  Scale timings use one unrecorded warm-up followed by 11
measured repetitions; we report medians and interquartile ranges (IQR).
The large-class stress leg uses the same repetition protocol.

We score a conflict only when the retrieved context contains a complete
minimal witness.  Context cost is serialized triple count; Table~\ref{tab:ret}
reports the smallest configuration that reaches 100\% conflict completeness
within a family.  Values are mean/maximum over conflict queries.  Since
fixtures and outputs are deterministic, we report exact counts rather than
significance tests.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{3.7pt}
\begin{tabular}{lrrrrrrr}
\toprule
Family & Instances/conflicts & Store & Exact witness & Key-aware & BM25 & Dense & Hybrid \\
\midrule
D20       & 14/8  & 133   & 2.0/2   & 4.0/4   & 7.5/8       & 7.8/8       & 7.4/8 \\
XR-small  & 16/12 & 345   & 6.0/6   & 8.0/8   & 83.0/83     & 345.0/345   & 163.0/163 \\
XR-scale  & 64/60 & 1,689 & 6.0/6   & 8.0/8   & 8.0/8       & 1689.0/1689 & 323.0/323 \\
Chain-2   & 41/10 & 440   & 9.0/9   & 94.6/98 & 440.0/440   & 440.0/440   & 440.0/440 \\
Chain-3   & 25/6  & 336   & 12.0/12 & 91.3/112& 336.0/336   & 336.0/336   & 336.0/336 \\
H3-NK1    & 16/12 & 405   & 6.0/6   & 9.0/9   & 99.0/99     & 405.0/405   & 195.0/195 \\
H3-NK3    & 16/12 & 525   & 6.0/6   & 11.0/11 & 244.8/254   & 525.0/525   & 493.3/495 \\
TauSig    & 60/12 & 648   & 10.0/10 & 12.0/12 & 220.2/226   & 648.0/648   & 430.8/433 \\
\bottomrule
\end{tabular}
\caption{Triples returned at the smallest fully conflict-complete setting
(mean/maximum). ``Exact witness'' is identical for maintained \cwi{},
on-demand exact, and positive adaptive exact output.}
\label{tab:ret}
\end{table*}

\subsection{Retrieval Results}

\paragraph{Exactness check.}
Maintained \cwi{} and the full exact equivalence join both produced
132 true positives, 120 true negatives, zero false positives, and zero false
negatives.  Every emitted witness was rechecked from its returned triples
without dereferencing the store.  In TauSig, the twelve positive cases include
the required metadata premises; 48 controls remain benign because their time
or scope cells do not overlap.  This is a conformance check for the
implementation and fixtures, not evidence that the supplied schema reflects a
real domain.

\paragraph{The strongest simple baseline is strong.}
Key-aware depth one returns every direct conflict in 4--12 triples.  Thus,
when identity is direct, a maintained witness index saves little context.
XR-scale also shows that BM25 can be ideal when the exact key string is present
in the query: its eight triples match key-aware retrieval despite a
1,689-triple store.  Dense retrieval is not uniformly competitive on exact
identifier matching.

\paragraph{Chains expose record-level expansion.}
For Chain-2 and Chain-3, exact witnesses contain 9 and 12 triples as predicted
by Eq.~\ref{eq:wsize}.  Key-aware retrieval must include the radius ball and
averages 94.6 and 91.3 triples.  All three relevance methods reach full
completeness only at their respective whole-store contexts (440 and 336
triples).  This does not show that learned retrieval cannot be trained for the
task; it shows that standard relevance ranking provides no completeness
contract on these controlled chains.

\paragraph{Maintenance does not shrink the certificate.}
\cwi{}, on-demand exact, and adaptive exact return the same shortest witness
on every conflict.  Therefore \cwi{} has no output-size advantage over fresh
exact computation.  Adaptive expansion also finds witness-sized positive
answers without maintained state, though it scans increasing layers and its
finite cap does not prove benign absence.  The operational choice is between
write-time maintenance, query-time exact work, and absence guarantees.

\subsection{Joint Budgets and Explicit Overflow}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{rrrr}
\toprule
$g$ & Shared sum & Shared union & Disjoint union \\
\midrule
1  & 6   & 6  & 6 \\
2  & 12  & 8  & 12 \\
4  & 24  & 12 & 24 \\
8  & 48  & 20 & 48 \\
16 & 96  & 36 & 96 \\
32 & 192 & 68 & 192 \\
\bottomrule
\end{tabular}
\caption{Multi-conflict triple budgets. ``Sum'' adds certificates without
deduplication; unions deduplicate shared premises and equal $b^\star$ in these
unique-path constructions.}
\label{tab:budget}
\end{table}

Table~\ref{tab:budget} shows both sides of the budget boundary.  The shared
family reuses four identity premises, so its exact joint cost is $4+2g$ rather
than $6g$; the disjoint family shares nothing and requires $6g$.  Across all
12 queries, \cwi{} returns every conflict at $B=b^\star$ and returns
\textsc{selection-overflow}, with an empty context, at $B=b^\star-1$.  This is
a contract conformance study, not a distributional claim about natural
queries.

\subsection{External Topology Audit}

To test whether key chains are solely a generator artifact, we separately
audit the CC-BY release of the established DBLP--Google Scholar
entity-resolution benchmark \citep{papadakis2022datasets}.  Its 5,347 gold
match edges connect 7,626 records into 2,351 components (maximum size 37).
Closing those gold edges transitively, 138 components contain 350 record pairs
whose two nonempty publication-year strings are unequal.  Shortest gold-path
depths are 1 for 157 pairs, 2 for 155, 3 for 23, and 4 for 15.  Thus 193
disagreement pairs are connected only through a chain, corresponding to
6-, 9-, 12-, and 15-triple logical witnesses under our encoding.

This is an exploratory topology check, outside the 252 confirmatory instances.
An unequal year is not an independently adjudicated semantic conflict:
publication versions, linkage mistakes, or source metadata errors may explain
it.  The audit supports only the narrow statement that transitive
co-reference and cross-source disagreement co-occur in an established
record-linkage benchmark.  It does not estimate conflict prevalence in agent
memory.  The artifact records source hashes and a built-in-only reproduction
script.

\subsection{Scaling Ledger}

We generate stores up to 100,000 persons (560,009 triples) and issue 100
queries sampled from 50 persons at each size.  Table~\ref{tab:scale} reports a
repeated-run companion to the registered deterministic ledger.  We omit a
cross-system timing claim because the maintained and on-demand methods pay
parsing, ingestion, and query work at different phases.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{rrrrr}
\toprule
Persons & Triples & Writes/triple & CWI ingest ms & 100 queries ms \\
\midrule
300     & 1,689   & 1.895 & 1.20 [0.89, 1.39]   & 0.75 [0.53, 1.14] \\
1,000   & 5,609   & 1.894 & 4.02 [3.36, 4.57]   & 0.49 [0.46, 0.64] \\
3,000   & 16,809  & 1.893 & 7.92 [7.69, 8.66]   & 0.39 [0.37, 0.42] \\
10,000  & 56,009  & 1.893 & 40.59 [39.23, 44.97]& 0.55 [0.52, 0.61] \\
100,000 & 560,009 & 1.893 & 672.20 [644.72, 838.01] & 0.67 [0.66, 0.69] \\
\bottomrule
\end{tabular}
\caption{Scale results: median [IQR] over 11 measured runs after one warm-up.
The query cell is a batch of 100 maintained-index lookups. No run had a
missing or spurious conflict.}
\label{tab:scale}
\end{table*}

Index writes remain 1.893--1.895 per source triple; at 100,000 persons the
index contains 440,006 entries.  The 100-query batch remains below 0.75 ms
median at every size.  These results support bounded update amplification and
fast lookup on this generator.  A separate adversarial class-stress leg gives
every record the same key and a distinct constrained value.  At 256 records it
materializes all 32,640 conflict pairs and ingest rises to 253.1 ms median
[245.8, 264.7], directly exposing the quadratic storage and update regime.
Neither leg establishes production latency, concurrency behavior, heap
residency, or a break-even query rate.

\section{Related Work}

Justifications and axiom pinpointing identify minimal premises for a fixed
entailment after access to the knowledge base
\citep{kalyanpur2007justifications,baader2010pinpointing}; diagnosis
uses minimal conflict sets \citep{reiter1987diagnosis}; and database provenance
tracks which source tuples support an output
\citep{buneman2001whywhere,green2007semirings}.  We reuse these objects and
incremental view maintenance \citep{gupta1993ivm}.  The distinction is the
quantified object: our property ranges over every relevant conflict and the
source assertions \emph{returned} under a context budget.
Equations~\ref{eq:cc}--\ref{eq:bstar} turn omission into a retrieval failure
and separate algorithmic overflow from information-theoretic infeasibility.
This is a contract-level contribution, not a new explanation enumeration or
inconsistency logic.

Long-term LLM memory work studies tiered context management and multi-hop
retrieval \citep{memgpt2023,hipporag2024}.  Sparse and dense retrieval optimize
relevance rather than worst-case witness inclusion
\citep{robertson2009bm25,reimers2019sbert}.  Reciprocal rank fusion combines
rankings without creating a logical completeness guarantee
\citep{cormack2009rrf}.  These objectives are complementary: relevance can
select candidate entities, after which conflict-complete retrieval can attach
checkable exception evidence.

\section{Limitations and Validity}
\label{sec:limits}

The strongest limitation is external validity.  Every confirmatory result uses
synthetic fixtures and an oracle schema.  A post-audit of exploratory public
data found that an earlier vulnerability analysis had incorrectly treated a
non-transitive ``related'' field as identity, while a clinical-trial probe had
treated shared grant identifiers as study identity.  We removed both
prevalence claims rather than substitute a weaker number.  The corrected
vulnerability alias graph contained no key chains and lacked semantic conflict
labels.  The DBLP--Scholar audit establishes chained gold-match topology but
does not validate its year disagreements as semantic conflicts.  We therefore
make no claim that the chain-conflict regime is common.

The formal fragment has one exact key per class, exact literal comparison,
single-valued constraints, interval overlap, and equal-or-unspecified scope.
It excludes learned entity resolution, composite keys, uncertainty
propagation, n-ary constraints, and open-world conflicts without finite
witnesses.  Confidence and schema are trusted inputs.  Record-snapshot
time/scope metadata is implemented, but arbitrary event histories are not.

The index is insertion-oriented; deletion, persistence, concurrent updates,
and crash recovery remain engineering work.  Per-conflict shortest witnesses
need not minimize their union, so \textsc{selection-overflow} is not proof that
$B<b^\star$; globally optimizing over alternative witnesses remains open.
The learned baselines use one frozen encoder
without task-specific training.  Finally, a complete witness guarantees that
a sound checker \emph{can} verify a conflict, not that a language model will
notice it.  Our exploratory reader runs confounded witness size with
distractors and used an outcome-defined competence subgroup, so we exclude
them from confirmatory evidence.

\paragraph{Generative-AI disclosure.}
Generative AI tools assisted with language editing, software and proof review,
claim-falsification checks, and submission-compliance auditing.  The authors
verified all text, proofs, citations, code, and results, take responsibility
for the complete submission, and list no AI system as an author or source.

\section{Conclusion}

Conflict completeness makes a missing contradiction a falsifiable retrieval
failure: every relevant conflict must arrive with one checkable witness, and
the minimum joint witness union states exactly when that demand can fit.
Fixed-hop IRI neighborhoods cannot guarantee it, while exact maintained and
on-demand methods can return witness-sized contexts or explicitly report
overflow.  Simple key-aware
retrieval is already competitive for direct conflicts; maintenance is most
useful for repeated queries over chained identity or when absence must be
certified.  Establishing whether those regimes occur in real agent memories,
under independently validated schemas and conflict labels, is the necessary
next step.

\bibliography{paper}
\end{document}
